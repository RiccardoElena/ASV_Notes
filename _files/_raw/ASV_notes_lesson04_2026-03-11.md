### Trascrizione - Parte 1

Una decomposizione, diciamo, più strutturale in termini di distribuzione della computazione in componenti. Quindi dobbiamo avere dei linguaggi e dei meccanismi che permettono di specificare sistemi grossi sfruttando questi approcci di decomposizione. Qualsiasi tipo di composizione dipende dalle circostanze, dalle necessità e dall'ambiente in cui stiamo operando. 

Quindi l'elemento base di questa, diciamo, decomposizione, i mattoni da cui partiamo sono questi, quelli che chiameremo Program Graph, che sono grafi di programma e che essenzialmente sono molto vicini a un linguaggio di programmazione. Poi vedremo quali linguaggi di specifica sono effettivamente molto vicini e sono usati per fornire, o sono in grado di fornire, delle primitive che nei modelli base del sistema non esistono. Nello specifico, un concetto di memoria vero e proprio, rappresentato dalla nozione di variabili ($\Var$) che possono mantenere ovviamente, contenere dei dati, e il valore dei dati può variare nel tempo. Può modificarsi nel tempo. Quindi è un po' la memoria, la memoria storica del processo. Quindi quello che ci interessa qui è iniziare ad avere un linguaggio che ci permette di modellare sistemi in cui il comportamento dipende dai dati.

Allora partiamo dall'insieme delle variabili che, in linea di principio, ciascuna delle quali può variare in qualche dominio. Potrebbero essere naturali, interi, caratteri... Quello che volete. È molto importante, alla fine dal punto di vista della modellazione cambia, come sapete, molto poco. Perché ogni dominio, a parte che sia diciamo così numerabile, può essere rappresentato come numero. Ogni dominio discreto può essere rappresentato da numeri naturali o da numeri interi, e quindi in realtà quale sia il dominio vero non è particolarmente rilevante, perché può essere tutto codificato in un unico dominio, in ogni dominio dello stesso tipo. Però quello che facciamo è per semplicità associare ad ogni variabile un dominio $D_i$, e poi prendere come dominio complessivo l'unione di questi domini, e quello sarà il dominio $D$ dei dati, sostanzialmente, su cui questo programma deve operare.

Ora, una volta che abbiamo stabilito l'insieme delle variabili, possiamo definire il concetto di programma su un insieme di variabili che, come vedete, ha una struttura molto simile, una sintassi molto simile a un Transition System, ma poi vedremo che ovviamente ha delle caratteristiche diverse. Nello specifico c'è questo concetto di variabile che non esisteva nel modello classico di Transition System.

Allora questa, diciamo così, effettivamente è la cosa più vicina a un programma, nel senso che, o meglio al concetto del programma. Quindi un Program Graph sta a, diciamo così, è l'analogo del programma; il Labeled Transition System era l'analogo della semantica del programma, cioè una rappresentazione dell'insieme delle esecuzioni del programma. Quindi, se volete, l'insieme delle esecuzioni del processo associato al programma. Quindi sostanzialmente questo oggetto qui sta descrivendo da un lato il flusso del controllo, e dall'altro il modo in cui, in questo flusso del controllo, il sistema opera sui dati esplicitamente.

Quindi il Program Graph è descritto da questi componenti, che sono insieme le operazioni del controllo, un po' come, diciamo così, il concetto di stato in una macchina di Turing. Sono gli stati del controllo o quelli di un automa. Invece, però, manteniamo la nozione di stato; in questo contesto sarà riservata allo stato, diciamo, sarà quello che nelle macchine di Turing chiamate "configurazione", quindi rappresenteranno lo stato dell'esecuzione. Le locazioni ($\Loc$) sono intese come le operazioni del controllo, quindi i numeri delle linee di codice di programma o, se volete, il dominio del program counter. È la stessa piccola cosa. Quindi questo sarebbe il dominio del program counter.

Poi c'è un insieme di azioni ($\Act$) che qui ha un ruolo, avrà una semantica. Sono intuitivamente le azioni che modificano lo stato durante l'esecuzione. Quindi dovremmo definire un qualche linguaggio per descrivere queste azioni e ovviamente cosa sia un'azione dipende dal tipo di sistema che voglio modellare. Quindi non c'è nessuna specifica a priori da parte di questi linguaggi su che cosa sia un'azione, ma sempre ci sarà un linguaggio per descrivere queste azioni. Questo lo vediamo dopo.

Questa $\hookrightarrow$ è una sorta di relazione di transizione simile, ma è una relazione che sostanzialmente serve a descrivere il flusso del controllo. Quindi, adesso la vediamo come definita, ma queste sostanzialmente saranno gli archi del grafo, questo oggetto qua.

Queste $\Loc_0$ sono le locazioni iniziali, cioè delle possibili locazioni iniziali di un programma per partire. Il $g_0$ è un'espressione che individua o che descrive la configurazione che le variabili devono avere in un qualsiasi stato iniziale. Quindi sostanzialmente adesso vedremo, ma questa è una condizione sulle variabili intuitivamente. Quindi questi due oggetti qua insieme stanno univocamente determinando l'insieme degli stati semantici iniziali, in quale configurazione. Se vogliamo usare il termine più adeguato, nelle macchine di Turing quali sono le configurazioni iniziali? Le configurazioni iniziali di una macchina di Turing sono determinate dallo stato iniziale della macchina di Turing e da una determinata configurazione della memoria. Quindi questa sarà una condizione sulla configurazione della memoria. Però questa è pure sintassi, nel senso che dovranno essere interpretati in qualche modo, dobbiamo dare la semantica.

Ora la relazione di transizione è una relazione che connette due locazioni tramite una condizione sulle variabili, che dobbiamo definire. È un po' come le azioni, questo insieme è dipendente dal tipo di linguaggio che io voglio usare per esprimere le proprietà. Quindi sostanzialmente se sto parlando di numeri, i miei domini sono numerici, le condizioni avranno come proprietà elementari dei confronti tra numeri che potranno variare. Però, di nuovo, in generale potrebbe essere altro. Potrei voler definire un concetto di condizioni con altri operatori che sono più adeguati al dominio o ai domini che voglio prendere in considerazione. Quindi dicevo, una condizione e un'azione.

### Trascrizione - Parte 2

Allora, intuitivamente, vabbè, quello che vediamo qui praticamente lo vedremo, ma intuitivamente qui l'idea è che ogni transizione all'interno è accompagnata da una condizione che viene tipicamente chiamata guardia. E l'azione è sostanzialmente quello che viene eseguito. Cosa? Quella transizione, l'effetto che quella transizione dovrebbe avere. 

Quindi la guardia determina le condizioni di abilitazione della transizione. Il sistema può eseguire quell'operazione, cioè quell'azione, e l'azione è esattamente quella che, intuitivamente, dovrebbe avere effetto su tutto lo stato interno. Quindi potete vederla, questa, come un *if-then-else*. Quindi c'è la locazione di partenza e la locazione in cui vado con l'*if-then-else*. Se la condizione è vera, eseguo l'azione nella locazione. Questo è un modulo, intuitivamente.

Ora, poiché $\Act$ è semplicemente un insieme di azioni, ad ogni azione dobbiamo avere modo di associare una qualche semantica. Questa funzione che aggiungiamo qui, $\Eff$, è una funzione il cui scopo è quello di descrivere la semantica delle azioni. Ora, la semantica delle azioni com'è definita? Allora, ovviamente, poiché è una funzione, diciamo così, che vuole calcolare una nozione semantica delle azioni, ovviamente questa funzione prenderà in ingresso l'azione a cui vuole dare la semantica. E l'idea è che questa funzione a quell'azione deve associare essenzialmente una relazione tra il valore delle variabili su cui l'azione opererà prima dell'esecuzione dell'azione e i valori delle variabili dopo l'esecuzione dell'azione. 

Cioè, questa funzione, se fissiamo l'azione, mi dirà come i valori delle variabili cambiano quando essa viene eseguita. Quindi mi serve il prima, e poi mi sta dicendo come le variabili, da un valore che hanno qui, vengono trasformate in un altro valore. Allora questo come viene fatto? Ovviamente abbiamo bisogno di introdurre un concetto di valori delle variabili, che è ovviamente un concetto semantico, e quindi questo insieme $\Eval(\Var)$ è l'insieme di tutti i possibili assegnamenti alle variabili. Vediamo se c'è scritto dopo, quindi lo vediamo dopo, ma intuitivamente questo è un insieme di funzioni, a sua volta, ciascuna delle quali associa ad ogni variabile un qualche valore del suo dominio. 

Quindi, ognuna di queste cose che sta qua dentro mi dice, per ogni variabile, qual è il suo valore. E quindi questa funzione $\Eff$ prende una funzione che è una possibile configurazione delle variabili del programma, del sistema, in un determinato suo stato semantico, e mi dirà, una volta che eseguo quell'azione, quale sarà la nuova configurazione delle variabili. Quindi questo qui è l'insieme di tutte le possibili configurazioni delle variabili. Questo sarà importante per definire la semantica, per definire questo oggetto qua.

Allora, le espressioni. Quindi, diciamo così, questi due oggetti qua, azioni e condizioni, sono definite a partire da un qualche concetto di espressione. Io vi do qualche esempio di possibile linguaggio per definire le espressioni, che è quello delle espressioni più frequenti. Le espressioni $\Expr(\Var \cup D)$ sono sostanzialmente dei termini che denotano dei valori del dominio $D$. E quindi l'insieme delle espressioni sulle variabili e il dominio mi servono entrambi, perché ovviamente voglio avere il concetto anche di costante. Quindi posso, se voglio, un'espressione, diciamo, potrebbe essere un'espressione che ha la sua semantica. E quindi le espressioni, per definire questo linguaggio di espressioni, ho come caso base variabili e costanti. Cioè, come diciamo che avviene. E poi devo avere degli operatori che mi permettono di comporre, come al solito, di costruire nuove espressioni a partire da espressioni più semplici. 

Allora, posso avere ad esempio come operatori gli operatori classici. In quel caso saranno le espressioni. Quindi a monte devo aver definito un linguaggio per delle espressioni sulle variabili e sulle costanti del dominio. Tanto questo possiamo definirlo insieme alle condizioni $\Cond$. Le condizioni, ovviamente, sono condizioni che vanno a testare qual è, diciamo, i valori delle variabili o di espressioni che contengono delle variabili. E ad esempio, se il mio dominio è numerico, allora le espressioni tipicamente saranno costruite a partire da, come dire, espressioni atomiche, che sono confronti tra espressioni, come abbiamo visto prima. Quindi una condizione atomica è un confronto tra due espressioni, come l'abbiamo detto prima. 

E poi ovviamente, in linea di principio, potremmo avere il concetto anche di combinazione booleana di espressioni atomiche, una sorta di linguaggio proposizionale le cui formule atomiche sono cose di questo genere. Quindi posso mettere in congiunzioni le condizioni, in disgiunzioni, posso fare implicazioni. Tipicamente quello che ci servirà è solo la congiunzione. Poi la disgiunzione sarà semplice, come viene e come può essere espressa. E la negazione, in realtà, la avremo solo sulle espressioni elementari, limitate sulle condizioni elementari. Quindi questo linguaggio di condizione, se volete, conterrà congiunzioni di condizioni elementari.

Un'azione, a questo punto, potrebbe essere ad esempio un assegnamento a una variabile o, più in generale, un assegnamento multiplo a più variabili. Io qua vi faccio l'esempio solo dell'assegnamento singolo, che è quello classico, per esempio nei linguaggi di programmazione. Quindi un'azione potrebbe essere una cosa di questo genere, che ti assegna a una variabile una qualche espressione di quelle che abbiamo introdotto ($v := \expr$). 

Quindi, a questo punto, abbiamo il linguaggio, abbiamo introdotto tutte le componenti che vedete qua, tranne questa, perché questa, come vedete qua, ha un carattere di tipo semantico. E quindi un esempio, poi vediamo come diamo la semantica a tutto.

### Trascrizione - Parte 3

Se prendiamo l'esempio che abbiamo visto qua come LTS, se vi ricordate, della *vending machine*, del distributore. Se riprendiamo quell'esempio e lo generalizziamo, quello che potremmo fare ora è, per esempio, abbiamo il concetto di variabile, possiamo arricchire il modello di prima introducendo un concetto in cui, per esempio, abbiamo due variabili $c$ e $t$. Dove $c$ effettivamente dovrebbe rappresentare la quantità di caffè a disposizione e $t$ la quantità di tè. Le locazioni sono una per ogni stato del controllo che vedete qua in verde, e le azioni, alcune azioni sono azioni nulle, che non hanno nessun impatto sullo stato, ma hanno un impatto sulla evoluzione dal punto di vista del controllo. E altre come queste, queste blu che vedete qui, che non sono aritmetiche, sono azioni fittizie in un certo senso. Semplicemente danno un nome alla transizione per passare da questo stato a questo stato. Se le omettessimo, non cambierebbe niente, perché avranno tutte la stessa semantica, lo stesso effetto.

Però, come vedete ci sono, nella sintassi che abbiamo usato qui, ad ogni transizione si associano due componenti, che sono la guardia e l'azione, per queste transizioni. Allora quella transizione da *start* a *select* intuitivamente è una transizione che può sempre essere eseguita. Quindi la sua guardia è `true`. Se è vero, o `true`, può essere eseguita e ha come azione *coin*, che semplicemente dovrebbe descrivere il fatto che una certa moneta è inserita. Ovviamente qua io non ho... potremmo avere qui una variabile che conterrà la quantità di credito presente, giusto? Non l'ho modellata, ma se aggiungessimo questa, potremmo dare a quest'azione una semantica non banale. Però per il tipo di stato che stiamo descrivendo con questa macchina, ovviamente non avrà nessun impatto sullo stato del sistema. Allora veramente non c'è nessun impatto.

Però queste altre transizioni, ci hanno a parte questa... queste altre transizioni ci hanno qualcosa di diverso, perché come vedete le guardie cominciano a non essere più banali. Perché questa per esempio, queste due transizioni hanno come guardia un test sulla variabile $t$ e si chiedono se $t = 0$ (cioè ha finito il tè) oppure $t > 0$. E quindi dice un'azione di decremento delle dosi, perché questa dovrebbe essere la transizione che eroga il prodotto. Se invece è zero si va in questo stato di errore, in cui in qualche modo viene restituita, si suppone, la moneta. E di nuovo, se avessimo il credito questo sarebbe un decremento di qualcosa, e questo è un decremento. E la stessa cosa avviene qua, perché è tutto simmetrico questa descrizione.

Ora, noterete che da un certo punto di vista, se lo guardiamo essenzialmente come una struttura a grafo, come è stato disegnato, questo oggetto qui è isomorfo a questo. Nondimeno, sono due oggetti completamente diversi! Perché questo qui non sta descrivendo... ma o meglio, sta descrivendo uno, ma molto diverso da quello che abbiamo visto prima. Addirittura, come l'ho descritto là sopra, che vi dice il dominio di $v$, per $v$ e $c$ o $t$ è sempre... questo Program Graph sta descrivendo un LTS con infiniti stati. Come farebbe un programma, diciamo isomorfo, o un programma classico essenziale peraltro, in cui abbiamo delle variabili, abbiamo degli *if-then*, eccetera eccetera. E chiaramente il numero di possibili stati di un programma è infinito, se il dominio è infinito. Anche se poi, quando viene eseguito, avremo un modello compilato e sicuramente l'insieme degli stati possibili sarà sempre finito, perché la memoria è finita nel calcolatore. Ma in un modello astratto sarà sempre infinito, e questo in qualche modo succede anche qua.

Questo oggetto qua può essere compilato automaticamente se abbiamo sufficiente informazione. L'informazione che ci manca per compilarlo è sostanzialmente contenuta in questa funzione $\Eff$, che adesso vedremo come usare. Poi innanzitutto vediamo come... quindi la semantica di questi oggetti viene data in termini di... quindi l'idea è che noi dobbiamo partire da un oggetto di questo genere (il $PG$) e ottenere un oggetto di questo genere (l'LTS). Quindi, cioè, perché questo è l'oggetto su cui possiamo effettuare gli algoritmi poi, che vedremo. Quindi la traduzione la potete vedere, o interpretare, come un linguaggio, ma anche come un... e come sarà definita, anche come un algoritmo di traduzione, o di compilazione se volete. Un compilatore in un certo senso dà semantica al vostro programma, lo traduce in qualche cosa che ha esattamente il significato del programma.

Allora, vediamo, per poter fare questa traduzione, ovviamente, che cos'è l'insieme degli stati dell'LTS indotto? Quali sono le azioni? Ma questo sarà praticamente semplice. Qual è la relazione di transizione? Com'è definita la relazione di transizione dell'LTS indotto? Quali sono gli stati iniziali? E qual è l'etichettatura degli stati?

Allora, gli stati è presto fatto. Coincidono con l'insieme che va... beh quasi, non proprio. Diciamo così, una componente importante di uno stato di esecuzione, o anzi, diciamolo direttamente: uno stato di esecuzione in un programma da che cosa è rappresentato? Dalle variabili, giusto? Ci sono due tipi di variabili: variabili esplicite e variabili implicite. Ok, le variabili esplicite sono quelle che stanno in $\Var$. E quindi ci serve sapere qual è una possibile valutazione delle variabili. Quindi ogni valutazione delle variabili dà luogo almeno a un potenziale stato del sistema. E poi ci sono le variabili implicite. La variabile implicita rilevante che stiamo considerando è il *program counter*. C'è una variabile implicita che non è... viene sostanzialmente da un'evoluzione, e questo sta però nelle locazioni $\Loc$.

Quindi l'idea è: una valutazione delle variabili è esattamente quello che vedete qua, è una funzione, anche se questo è l'insieme delle, diciamo, una parte delle possibili valutazioni delle variabili quando le variabili sono $a$, $b$ e $c$. Ma essenzialmente è una... non ce l'ho scritta formalmente, no, non l'ho scritto tantissimamente, ma significa una cosa, si vedrà. Allora, una valutazione è una funzione che va dalle variabili al dominio. È abbastanza ovvio. Ma questo oggetto qui, questa funzione, è un modo per ottenere per ogni variabile il suo valore. E quindi è esattamente l'informazione che rappresenta lo stato della memoria. Quindi allora, lo stato della memoria in qualsiasi stato dell'esecuzione è rappresentata da una funzione dalle variabili al dominio. Ovviamente se l'insieme delle variabili è finito, sarà chiaramente una tupla la cui lunghezza è pari al numero delle variabili.

E quindi questo mi dice che $\eta(a)$ è uguale a 0, $\eta(b)$ è uguale a 0, $\eta(c)$ è uguale a 0. Questa è la prima $\eta$. Questa è un'altra $\eta$. $\eta'$ mi dice che $\eta(a)$ è uguale a 1, $\eta(b)$ è 0, $\eta(c)$ è 0. Eccetera eccetera. Quindi ciascuna di queste triple è una di queste funzioni qua. Quando $\Var$ è questo esempio, cioè, io qui spero sia chiaro. Quindi questa, diciamo, $\Eval(\Var)$ è l'insieme di tutte le possibili $\eta$. Ok? Allora, cioè l'insieme di tutte le possibili configurazioni della memoria, è tutto il dominio delle possibili configurazioni.

Quindi, ecco, una volta che abbiamo questo è chiaro che una coppia $\langle l, \eta \rangle$ è uno stato, rappresenta uno stato di esecuzione del sistema. E ovviamente questo, vediamo se è scritto nella slide e non vediamo questo tutto anche il PG... Ma no, questo è troppo informale, ok per poi lo facciamo a pezzi. L'insieme degli stati dell'LTS è preso dalle locazioni del controllo e dalle valutazioni. Cioè, ognuno degli oggetti, ogni elemento che sta qua, ha questa forma qui. Perché questa $l$ appartiene a $\Loc$ e questa $\eta$ appartiene a $\Eval$. Quindi questi qui sono quegli stati semantici. Ok.


### Trascrizione - Parte 4

A questo punto, però, che abbiamo il concetto di $\Eval(\Var)$ definito, possiamo finalmente dire che cos'è $\Eff$. Perché è quello che mi permetterà di dire, diciamo, temporalmente... cioè, la relazione $\hookrightarrow$ mi dice quale locazione si può muovere in quale altra locazione. Quindi mi sta dicendo il controllo come evolverà. Questo $\Eff$ è l'analogo di questa relazione di transizione, ma è quello che mi permette di connettere stati dei dati, configurazione dei dati in configurazioni dei dati a fronte dell'esecuzione di un'azione. Mi dice come evolveranno i dati. 

Quindi questo è ad esempio: se ho preso un'arbitraria configurazione dei dati $\eta$ e un'azione $a$ della forma $v := \expr$ (assegno alla variabile $v$ una certa espressione $\expr$), nella valutazione $\eta$... scusatemi, che significa? Che cosa significa calcolare qual è la valutazione delle variabili che ottengo eseguendo l'azione $a$ quando lo stato della memoria è $\eta$? Quindi, ovviamente, di assegnamento. Quindi quello che, se questa è l'azione, allora cosa sarà? Così non va bene, ci serve un'altra... qui c'è un po' di... perché sento così il problema. Si ha una valutazione $\eta$. Questa notazione qual è? È la notazione di assegnamento, sostanzialmente di sostituzione al valore di $v$ in $\eta(v)$ del valore, cioè una volta. Quindi potrebbe tornare inutile perché questa parola non è adeguata per queste cose. Allora, vediamo che riesco comunque a fare le cose per bene. 

Allora, $\eta[v \leftarrow \expr]$ (dove l'espressione appartiene al dominio), questa roba qui è una nuova valutazione, è uguale a $\eta'$ definita in questo modo. $\eta'$ è definita come: di una variabile $x$, se $x$ è uguale a $v$, è uguale a $\expr$; altrimenti è uguale a $\eta(x)$. Quindi questa è un'operazione di trasformazione di una valutazione in un'altra valutazione che lascia tutte le variabili così come sono. Definire questa $\eta'$ qua è che $\eta$ associava qualche cosa a $v$, mentre $\eta'$ gli associa proprio $\expr$ qui. 

Ora, per essere precisi, quello che dovremmo rifare qua è avere un concetto di valutazione dell'espressione che dovremmo definire, ma che dovremmo definire. Cioè, una $\eta$ mi dice ogni variabile che valore ha, giusto? Quando io ho un'espressione che contiene variabili e c'ho una $\eta$, posso chiedermi quell'espressione che valore ha in quella... rispetto a quella $\eta$, giusto? E per $\eta$ diversa avrà un valore diverso, no? Una variabile in uno stato può avere un valore, un'espressione in uno stato per l'esecuzione del programma dovrà avere un valore e un altro. Per cui ci serve un concetto di $\Val(\expr, \eta)$ con qualche modo di ottenere il valore di un'espressione rispetto a $\eta$. E intuitivamente questo deve essere definito induttivamente sulla struttura delle espressioni. Il caso base saranno quali? I casi base delle espressioni che sono variabili e costanti. Quelle sono... le costanti sono valutate in loro stesse. Questa, diciamo così, mi serve per il caso base delle variabili. Dopodiché, tutto il resto avrà a che fare con l'interpretazione degli operatori. 

Quindi, se l'espressione è $v + 1$, la valutazione di $v + 1$ rispetto a una certa $\eta$ sarà il numero corrispondente a $\eta(v) + 1$, qualsiasi questo sia. In un'altra $\eta$ sarà un altro numero, che sarà $\eta'(v)$ tutto quanto più uno. Quindi per essere precisi, io qui avrei dovuto mettere esattamente questo. Quindi l'effetto associa il valore che l'espressione $\expr$ aveva in $\eta$, cioè nella valutazione di partenza, cioè rispetto a questa. Mi è difficile, dovrei scrivere precisamente dove avrei bisogno di scrivere. Questo vorrebbe essere: è uguale a $\eta[v \leftarrow \Val(\expr, \eta)]$. A questo e quello che ah... sarebbe correttamente da scrivere qua, che poi è la semantica degli assegnamenti, di diverso da quello che c'è qua. Quindi, per esempio, se $\eta(v)$ è uguale a $5$, allora l'effetto di questa istruzione in quella $\eta$ sarà la $\eta'$ tale che il valore di $v$ nella $\eta'$ è $6$. Tutto il resto è rimasto come era prima. Quindi niente di sconvolgente. 

Allora a questo punto, considerando diciamo così, quello che dovremmo fare è definire la relazione di transizione adesso. L'insieme degli stati sarà della macchina venditrice, del distributore... come distributore sarà questo infinito insieme di stati se comincio, ovviamente, in cui vedete ogni stato contiene sia la locazione in cui si può trovare il sistema che una possibile $\eta$. Ok, questa qui, la $\eta$ che assegna $c=10$ a $t=3$, questa $c=3$ e $t=10$, e $c=11$ e $c=12$. Ora dovremo definire la relazione di transizione, giusto? La relazione di transizione ovviamente dovrà tenere conto sia del fatto che il controllo si deve muovere seguendo certi vincoli, sia del fatto che le azioni che si eseguono hanno certi effetti. Quindi mi devo chiedere: ma quali sono le coppie di stati che stanno in relazione semantica? Cioè, da quali stati mi posso muovere in quali stati? Questo ovviamente sarà una combinazione della relazione di transizione del Program Graph ($\hookrightarrow$) e della funzione $\Eff$ che abbiamo appena discusso. E l'idea è sostanzialmente quella che vedete qua, allora. 

Quindi se prendiamo semplicemente la definizione della funzione di $\Eff$ nell'esempio che stavamo facendo, e quindi qua vedete, questo mi dice che le reazioni a $t$ per esempio sono azioni che non hanno diviso prima. Quindi dice che per ogni variabile, l'effetto, la $\eta'$ di questa funzione modifica come abbiamo detto... questo decremento il valore della variabile, scusatemi, e lascia tutte le altre parallelamente per la variabile. Ora dato questo, possiamo cominciare a dire come è definita questa relazione di transizione $\xrightarrow{}$. 

Quindi ricordatevi che $\Act$ nei Transition System è un'etichetta dell'arco, quello perché tutta l'informazione che c'è nella evoluzione è... o che è coinvolta nella definizione dell'evoluzione del sistema sta nelle coppie di stati che sono in relazione. L'etichetta in $l'$ e $\eta'$, ok? Quindi c'è questo oggetto qui, $\langle l, \eta \rangle \xrightarrow{a} \langle l', \eta' \rangle$, che appartiene alla relazione di transizione di $S$ se queste condizioni sono verificate. Cioè, se tra $l$ ed $l'$ con una certa guardia e una certa azione, ok? Se una cosa di questo genere appartiene al Program Graph... cioè, questa cosa così, se esiste nel Program Graph una transizione di questo genere, $l \xhookrightarrow{g:a} l'$, e in più se i dati soddisfano la guardia $g$ della transizione ($\eta \models g$), ed $\eta'$ è l'effetto di eseguire l'azione $a$ della transizione del Program Graph ($\eta' = \Eff(a, \eta)$). Cioè, eseguendo l'azione, se a $\eta$ lo stato corrente soddisfa la guardia, quindi la transizione è abilitata, e a... la configurazione dello stato di arrivo è ottenuta eseguendo l'azione $a$ in questa...

### Trascrizione - Parte 5

Sto modellando il fatto che il sistema si trova in uno stato, ha tante possibili transizioni $PG$ da fare. Quali di queste potrà eseguire il programma? Sicuramente una di quelle da cui guardia... questa, ci deve essere la transizione della guardia nella $\eta$ corrente e a... se nella $\eta$ corrente, lo stato corrente, eseguo l'operazione $a$, il risultato sarà lo stato di un grafo. No, perché ci sono tante possibili transizioni $a$ nella sintassi, ok? Cioè, io mi trovo in uno stato e se è vero questo fai questo. Quindi non è che se la... sì, esatto, e già esiste una relazione nel grafo da sinistra a destra, mi dice solo che se eseguissi quella, se fosse possibile eseguire questa transizione, quella transizione farebbe inizia... come cambiano i dati, ok? Io adesso ho tutte le possibili configurazioni, poi lo vediamo. Prendo una coppia di configurazioni, degli stati, e mi chiedo: tra questi dati, nello stato che scelgo, deve essere quello... sì, lo stato non sarà generabile. Poi vediamo nella condizione dichiarativa mi dice: ci saranno queste... una relazione se sono vere queste cose. Se una di queste cose non è vera, non è detto che ci sia. Se non c'è una transizione che ha queste proprietà tra quegli due stati, non c'è una transizione tra queste... questi, il sistema non può andare in un passo da qui a qua, perché ci dovrebbe essere... Ora c'è una, ecco, esiste una transizione a... quindi da *start* a *select* con guardia `true` c'è. Peccato che da qui a qui non ci arriva con l'azione di quella transizione, queste due $\eta$ sono diverse. Quindi eseguo l'azione *coin* che farebbe passare da *start* a *select*, poiché il suo effetto è: lascio le variabili che sono... la $\eta$ che sta qui in blu è effettivamente uguale a questa $\eta'$ che sta di là. Di conseguenza quella coppia soddisfa la condizione. Ma da qui, esatto, la stessa cosa non è possibile a quello, perché non c'è proprio una transizione che mi porta da *start* a *start*, e quindi quella è facile perché, dai, il controllo che me lo impedisce. Però da qui l'unico stato in cosa andare...

Qui, iniziali sono quelle coppie locazione e variabili, tale che è una locazione iniziale ed è una di quelle valutazioni che soddisfa la guardia iniziale $g_0$. Questa condizione è stata veramente... tutte le variabili sono iniziali. Ad esempio, una classica in un programma standard, un programma imperativo classico, $g_0$ è praticamente quella condizione che dice "tutte le variabili sono inizialmente uguali a 0". Io non ve l'ho definita, ma questo concetto di soddisfazione $\models$ è sempre il solito concetto di soddisfazione, se volete, in logica del primo ordine senza quantificazione. Perché quando andiamo a... la quantificazione... quindi ovviamente anche per questo simbolo di questa relazione di soddisfazione deve essere definito intuitivamente sulla struttura di queste condizioni. E quindi quello dipende da come... qual è il linguaggio delle condizioni. Intuitivamente è abbastanza naturale definirlo una volta che ho il linguaggio delle condizioni definiti.

E infine l'etichettatura $L$. Allora per l'etichettatura, come potremmo... ogni locazione $\Loc$ del sistema è una proposizione atomica, per ogni condizione atomica nel sistema, quindi in realtà vorrei parlare solo di quelle atomiche... e ovviamente questo è potenzialmente finito, non è detto che sia finito. L'idea intuitiva è che la locazione sarà, o meglio la proposizione atomica associata alla locazione sarà vera in quegli stati che sono in quella locazione. Quindi, per esempio, la proposizione atomica *start* sarà vera in questo stato, ma sarà falsa in questo stato, nel quale invece sarà vera l'altra proposizione atomica associata alla locazione di stati che la contengono come locazione. E poi ogni condizione è valutabile rispetto ad uno stato semantico, semplicemente andiamo a questo premio... valutando il valore delle variabili, applicando... Diciamo, per uno stato $\langle l, \eta \rangle$ le proposizioni atomiche che sono vere sono $l$ stessa, unica proposizione di locazione che è vera, più tutte le condizioni atomiche che sono soddisfatte dalla memoria. Quindi mi dice sostanzialmente l'etichettatura di uno stato: mi dice in che locazione si trova quello stato e quali sono le proprietà delle variabili vere di quello stato sostanzialmente. L'etichettatura per trasformi... quindi vedete che sono così. Il modo in cui viene generato il... viene generata questa roba qui vi ricordate? Cioè, viene costruito un insieme di proposizioni atomiche e diciamo è coerente, non è più arbitraria, ma è coerente con la specifica del sistema codato. Quindi di nuovo queste sono effettivamente ragionevoli proposizioni. Questa è un modo per indicare nell'algoritmo di traduzione che garantisce che quella proposizione atomica sarà vera solo negli stati che corrispondono agli stati del sistema in cui... e quindi lo garantisce il meccanismo di compilazione che poi questa proposizione atomica avrà il significato o rappresenterà effettivamente quello che voleva. Ma dal punto di vista dell'LTS una proposizione atomica vale un'altra, non c'è un significato speciale, quello è un significato che deve venire dalla specifica del sistema. Ok?

Quindi ora qui per esempio fa vedere come si... un esempio della traduzione. Questo è l'input della traduzione del Program Graph. Queste sono le definizioni delle funzioni eccetera, e queste sono le transizioni che verrebbero inserite nell'LTS corrispondente. Quindi, per esempio, come abbiamo visto, da *start* le variabili non cambiano, e da *select* a *t* con azione *t* si può andare con guardia `true`. E poi da *t* vedete posso andare in *start*, posso tornare in *start*. Ma questo tornare in *start* è un ritorno a *start* come locazione, ma non come stato! Non ritorna allo stato da questo partito. Perché questa transizione... per la transizione, la modifica ha una guardia che mi dice $t$ deve essere maggiore di zero, e qui è soddisfatto perché $t$ è uguale a tre, ma poi ha come effetto il decremento del valore associato a $t$. E quindi ovviamente da questa valutazione vado a finire nella valutazione di tutto uguale, ma $t$ è diventata due! Ok? Quindi questa è la transizione, esisterà. E poi da qui vanno qua, così, in un infinito LTS. Questo se vi ricordate era un po' quello che abbiamo cominciato a discutere il concetto di quantità, giusto? Ma se avessimo introdotto il concetto di quantità avremmo dovuto replicare tante volte quelle LTS in modo tale, e connettendo una coppia con un'altra... in modo, partiamo da una specifica che ha la stessa dimensione di quella del distributore più piccolo, ma la cui semantica nello specifico è un LTS infinito. Ovviamente l'infinitezza di questo... il modo di affrontare questo problema... sono vari modi per provare a provare questo sistema, sicuramente quello di maggior successo è quello di assumere un dominio infinito. Allora adesso il linguaggio per modellare è molto più semplice, mi permette di descrivere un sistema molto grosso, un sistema di esecuzioni è molto assicurato, usando un linguaggio molto compatto che è: usiamo un linguaggio di programmazione con le variabili invece che descrivere stato per stato l'evoluzione del programma nel possibile sistema. Quindi è lo stesso spirito qui di questo, va bene.

C'è un problema con la semantica della transizione qua, c'è qualche altro esempio mi sembra che fa vedere esattamente chiaro il fatto che esattamente partendo da un sistema potenzialmente iniziale, quindi c'è lo stato zero, se $g_0$ non ammette 0 come possibile valore iniziale ovviamente non sarà uno stato iniziale. Ma questo stato, se arriva a questo stato, si muove in questo stato, ma l'unica transizione abilitata in questo stato non sarà quella di un altro stato, ma l'unica possibilità è come ho scelto il $t$ e l'unica transizione... questo stato non sarà un altro stato, in questo sarà un altro stato. Ma ovviamente è possibile scrivere... questo lo farete con il professore, non sarà facile per scrivere una proprietà che vi dice che questa macchina non è robusta e a un certo punto può terminare di fornire, o meglio come è definita è possibile che da un certo punto in poi non dia mai niente per esempio. Ecco, già vedete il tipo di proprietà che voglio: parla del "è possibile che in futuro qualcosa succede". Nel futuro come ho detto io non sarà neanche esprimibile necessariamente nel primo linguaggio, che questa non è una proprietà lineare. Vi ha detto qualcosa spero il professore sul concetto di tempo: "è possibile che qualcosa succeda" è intrinsecamente *branching*, che è possibile esista un'esecuzione che ha certe proprietà. Quindi sto immaginando in futuro tante possibili evoluzioni, questo è il concetto. Invece le proprietà lineari parlano di una singola esecuzione. Quindi in una presa, un certa esecuzione, qualcosa può succedere? Quello dipende: questo "essere possibile" come lo voglio interpretare? Se è possibile nel senso di "è possibile in questa esecuzione trovarmi in uno stato che ha certe proprietà", oppure se è possibile prendere un'esecuzione... "esiste una possibile esecuzione che ha certe proprietà", ok? E questo ovviamente, per poter esprimere questi due concetti ci vogliono linguaggi diversi. Poi ci sono alcuni linguaggi che possono esprimere entrambi, ma questo però giusto per avere l'idea del... in principio linguaggio per poter distinguere questo sistema che non è corretto. Perché una delle specifiche per esempio che io voglio è che la macchina venditrice sia sempre in grado di fornire le bevande in qualche modo, magari non eseguendo delle operazioni di rifornimento per esempio. Però il meccanismo di errore che abbiamo visto qui non garantisce questa proprietà che potrebbe essere... Ok.


### Trascrizione - Parte 6

Allora questo qui è sostanzialmente il modello di base, perché abbiamo visto il linguaggio per definire i componenti, e finora diciamo così quello che riusciamo a esprimere in questo linguaggio è poco più di un programma sequenziale. Poco più di... più di un programma sequenziale. Quello che questo linguaggio riesce a dare più di un classico linguaggio di programmazione è la possibilità di scelte non deterministiche. Cioè la possibilità, come diciamo così, queste due transizioni sono possibili indipendentemente dallo stato, quindi c'è una scelta, non è determinata dallo stato corrente qual è lo stato successivo.

Ovviamente potreste dire: "Però si può discriminare tra queste due transizioni che è l'azione, il nome dell'azione associata". In realtà, come dire, l'azione è solamente un nome, non ha una semantica speciale in linea di principio. Non è neanche un'azione se vogliamo essere, diciamo, coerenti con l'interpretazione ovvia della macchina venditrice: questa non è un'azione che è sotto il controllo della macchina, quindi lei decide. C'è un intrinseco non determinismo nel comportamento della macchina che è dovuto all'interazione con l'ambiente.

Questo ovviamente va oltre la possibilità di esprimere scelte non deterministiche dentro il sistema, perché ogni scelta deve essere univocamente risolta all'interno, altrimenti non è possibile l'esecuzione. Ovviamente noi siamo in una situazione in cui... cosa ce ne possiamo fregare dell'esecuzione? Noi non avremo mai il tipo di problema che vogliamo risolvere con questi linguaggi qua. Da un lato non ci interessa risolvere necessariamente il determinismo, e spesso non lo vogliamo neanche fare, perché quello che vogliamo fare è permettere di esplorare, o avremo modo di esplorare, tutte le possibilità. Cosa che quando siamo in un sistema non è possibile fare. È possibile simulare, ma è possibile simulato, per esempio esploro l'esecuzione tutto indietro eccetera eccetera. Questo è il modo in cui uno dovrebbe... e questo, se ricordate, è in qualche modo una macchina deterministica può per esempio... se avete visto il teorema, ma quello per esempio cosa vi permette di fare? Facendo vedere come posso simulare una macchina che lavora con uno spazio non deterministico, un salto che non cambia la classe di complessità, giusto? Ok. E perché diventa quadratico sostanzialmente? Per il quadrato l'uso... ha fatto che devo tornare indietro e simulare tutte le... delle scelte.

Ovviamente la presenza di non determinismo è particolarmente utile per noi, che vedremo anche quello serio. Quale vantaggio ha il non determinismo? Ha vantaggi o svantaggi? Discuteremo. Un vantaggio principale: nella via d'astrazione maggiore sarà il livello di non determinismo, avrò meno modo di discriminare le scelte possibili del sistema. Un po' per farmi capire bene la questione prendete questo. Questo è, diciamo, lasciando perdere le etichette, semanticamente questo è il classico esempio di un sistema estremamente non deterministico, perché quando mi trovo in questo stato ho sempre la possibilità nel sistema. Ora, se andiamo però al livello di astrazione altissimo, perché io quando parlo non c'è il concetto di quantità, quindi questa è la versione più astratta possibile che preserva però il controllo, l'evoluzione del controllo.

Nel momento però in cui io vado a mettere informazione esplicita sulle variabili, il sistema diventa enorme, come questo. E vedete che questa transizione qua... o ho la possibilità di fare quella che va in errore, una sola delle due è possibile, perché questo sistema, questa descrizione ha sufficiente dettaglio per risolvere. Il sistema obbliga ad aggiungere informazione e a rendere il sistema molto più grande. Quindi il livello, se vogliamo mantenere o regolare il livello di astrazione, quindi mantenere uno strumento, verificare la proprietà, diventa fondamentale avere uno strumento, diciamo una manopola, per l'appunto per regolare questa dimensione del sistema. E questa regolazione però è possibile solo se... rialzare la regolazione. Quindi gli aspetti positivi sono quelli che permettono di avere una descrizione più o meno astratta del sistema; di contro avrà che chiaramente questo non determinismo ammetterà dei... perché non c'è nessuno di $t > 0$, in particolare il tè non è zero, però diciamo il modello astratto che avevamo all'inizio permette, come quello, permette sempre di andare in errore. Quindi ci sono delle proprietà che sono falsificate là e verificate qua, per potenzialmente il vizio d'astrazione. Quindi tendenzialmente uno vuole mantenere un certo lato nella questione, ma una proprietà che è falsificata nel sistema astratto potrebbe essere verificata nel concreto. Quindi questo semplicemente perché chiaramente io vado alla ricerca di errori, e se l'assenza di errori è garantita nel sistema concreto... e se però c'è un errore nel sistema astratto, posso ammettere che questo esiste nel sistema concreto? Perché ho modo per esempio di provare una simulazione, è possibile, perché in realtà il sistema ha delle informazioni in più che lo scarterebbe questo comportamento. Allora ho modo di regolare il livello di astrazione nella descrizione facendolo scendere, quindi aggiungendo un po' più di informazione, cercando sempre di non far esplodere la semantica, cioè il grafo sottostante, ma escludendo i comportamenti che sono spuri.

Ora però vi dicevo, ora diciamo che questo linguaggio per ora non permette poco più della descrizione dei sistemi sequenziali. E però uno dei sistemi complessi è quello di decomporre i componenti che interagiscono. Quindi che non solo interagiscono tra loro nel senso sequenziale, ma potenzialmente anche comunicano tra loro, quindi eseguono in... vuol dire due sistemi concorrentemente. E soprattutto qual è... come faccio, avendo le descrizioni dei sistemi in isolamento, a costruire una descrizione del sistema concorrente risultante dalla loro combinazione prima? L'idea dovrebbe essere come questa e, diciamo, costruire la descrizione di un sistema concorrente nella sua interezza è estremamente complicata, perché devo poter prevedere tutti i possibili modi in una esplosione. Nella prima lezione c'è un'esplosione esponenziale sul numero dei processi paralleli o dello spazio degli stati. Quindi una specifica diretta in termini di LTS è molto complicata. Per cui l'idea è quella di avere degli automatismi, cioè partire dalle descrizioni isolate e avere degli operatori di composizione parallela che mi permettono automaticamente di generare la descrizione del sistema unito qua. Perché quello che abbiamo scritto qui in termini non operazionali è una procedura di costruzione dello spazio dell'LTS, che ovviamente, se questo insieme di oggetti è finito l'insieme qua, e poi in ogni coppia fa verificare se è nella condizione che abbiamo dato: se è vera ci mette un arco, se non è vera no. Quindi è automatizzabile questa roba sui sottosistemi. E già ha delle complicità, perché ovviamente io posso ammettere composizione sincrona, composizione asincrona, composizione parzialmente sincrona, controllata o sincrona solo in base a comunicazione esplicita, quindi scambio di messaggi.

Un circuito decomposto in sottocircuiti, ognuno dei quali è descritto da un Program Graph ($PG$) per esempio, quando faccio la composizione mi aspetto, per le caratteristiche del circuito, che la composizione... quali componenti possono eseguire la loro transizione, e la stessa cosa potrebbe avvenire, avviene. Quindi la differenza sostanziale che uno può avere in mente tra un sistema in cui il comportamento è dettato in maniera sincrona, e quindi tutti possono agire solo in determinate istanze, oppure in modo del tipo asincrono. E uno dei possibili, classico contesto è in un sistema distribuito, in cui ho più, diciamo così, più unità di computazione che sono autonome, eseguono autonomamente processi e questi processi possono eventualmente comunicare tra loro. Ma le relazioni... non può il sistema decidere di sincronizzare alcuni componenti sotto certe condizioni e altri no. Ovviamente il sistema distribuito, un sistema diciamo completamente distribuito in cui non c'è nessuna composizione parallela, non è determinata, può essere definita in tanti modi. Quindi a seconda di come la definisco, poi avrò una traduzione diversa, una composizione diversa dei processi che voglio specificare e che voglio comporre.

Allora, il più semplice modello di composizione parallela è quella sincrona. E la composizione parallela sincrona è molto semplice perché la sua definizione formale partendo dal modello di LTS... quindi ho già degli LTS costruiti, uno per ogni sottosistema che voglio costruire. Nel caso specifico io ho $n$ sistemi, $n$ componenti, modelli componenti ciascuno. Ora qui ci concentriamo sulla parte strutturale, l'etichettatura è una cosa che è molto semplice da associare, quindi la parte interessante è la struttura dell'LTS sottostante. Quindi come evolvono? Devo eseguire, quindi si muovono tutti o evolvono in maniera sincrona, seguendo intuitivamente uno stesso clock. Allora in questo caso la composizione modella la costruzione dell'LTS corrispondente, cioè che rappresenta il modello complessivo. È una semplice... avviene tramite prodotto cartesiano, perché l'idea è quella di descrivere ogni stato come una tupla, nello specifico ogni elemento della quale corrisponde allo stato in uno degli LTS. Quindi è un grosso stato globale fatto di stati locali. E quindi l'insieme degli stati del prodotto sincrono, nella composizione sincrona... degli stati locali dei sottosistemi. Un'azione qui viene molto naturale, perché un'azione globale, poiché tutti eseguono contemporaneamente un'azione, un'etichetta diciamo così, che sarà una tupla di azioni locali, una per ogni sistema. Gli stati iniziali, ovviamente, sarà una tupla di stati iniziali locali. E la relazione di transizione, ovviamente, sarà definita nel seguente modo: quindi tra due stati, ciascuno dei componenti, esiste una transizione che porta dal corrispondente stato nella posizione $i$-esima dello stato globale di partenza, lo porta nello stato $i$-esimo dello stato globale di arrivo, utilizzando l'azione $i$-esima locale $a_i$ all'interno della relazione globale. Quindi questo dicendo sostanzialmente che deve essere possibile per $s_1$ andare a $s_1'$ con $a_1$, il secondo da $s_2$ deve poter andare da $s_2$ a $s_2'$ con $a_2$, eccetera. Non devono essere per forza uguali $a_1 \dots a_n$, qui sono azioni locali, non devono essere per forza uguali, giusto? No, basta che ogni $a_i$ appartenga alle azioni $A_i$. Cioè in che senso deve essere per forza uguale? Ma di nuovo, questi oggetti qui sono non deterministici in linea di principio, quindi da uno stato, se c'ha tante azioni che può scegliere, una qualsiasi... l'azione che lui sceglie può scegliere una qualsiasi. In questo non deve essere per forza uguale. 

Allora, di... se c'è... quale viene... che so, $a_1$, $b_3$ e $a_1$, $c$ altre che sono due azioni globali, perché c'è solamente l'azione $a_3$, ma due c'hanno due azioni possibili che sono 1, 0. Ovviamente è possibile che io da uno stato posso andare, sì, da uno stato, facendo uno stato globale o facendo questo vado in uno stato globale, facendo questo vado in uno stato globale. Se c'è non determinismo nei Transition System di partenza, questo non determinismo verrà ereditato di certo. Come dire, il sistema sincrono non aggiungerà un non determinismo che non esiste già. Perché? Perché è molto vincolato, il modo in cui il parallelismo non è un parallelismo del tutto libero. Perché ovviamente... sì, quindi è molto restrittivo, però è in un certo senso un concetto di concorrenza reale. Perché così è possibile, questi componenti però devono muoversi sempre insieme. E ovviamente, capite bene, già qui, nonostante l'insieme dei comportamenti non sia necessariamente molto complicato, l'insieme degli stati sta diventando enorme. Quindi avere un automatismo per costruirlo è fondamentale, perché il problema che noi qua ci stiamo ponendo è dal punto di vista del modellista. Quindi abbiamo bisogno di linguaggi e strumenti per avere delle descrizioni umanamente fornibili, producibili, di sistemi molto complessi. Quindi è fondamentale questi meccanismi di automazione della costruzione poi del modello esteso, perché altrimenti non andiamo.

**Studente:** Nessuna transizione in $R$... non mi è chiara la parentesizzazione diciamo dell'AND, nel senso dopo la virgola va letta come ogni...
**Professore:** Allora questa esiste se per ogni $a$... non so, è scritto bene? Questo "per ogni" sta solo qua, per ogni sono per il... ok, questa data una certa $a$ appartenente... ok, qualsiasi $a$ appartenenti all'$A$ grande, questa qui per ogni transizione se per ogni vale questo. Il "per ogni" sta qui, qua c'è tutto universale, esatto. E questo sto dicendo: per ogni tupla di stati, per ogni azione globale, diciamo questa tupla in $A$, e possiamo considerare una variabile, una variabile legata implicitamente...
**Studente:** Ok, allora questa è legata, cioè non è libera, ok.
**Professore:** Non è libero, ok. Questa dopo libera...
**Studente:** No, questa sembrava ridondante come condizione rispetto al... però che basta che sia uno... no, è che mi stavo chiedendo a questo perché inizialmente l'avevo interpretata come libera perché anche quella quantificata universalmente. Ma lì mi chiedevo a quel punto perché mi sembrava ridondante come condizione rispetto al fatto che esistessero le singole sottotransizioni. E mi è venuto il dubbio che potesse esistere un elemento, una tupla che non era corrispondente a delle singole sottotransizioni dei sottosistemi, che però non mi sembra il caso nel senso... la tupla di queste, perché quelli non ci sono tutte le azioni globali.
**Professore:** No, in realtà se esiste... il punto è che avere l'azione e la transizione, nel senso, io potrei avere quest'azione globale ma esistono tutte le singole $s_i \xrightarrow{a_i} s_i'$. Necessariamente la tupla esiste in $A$, no? Anche se non c'è, tra queste alcune di queste non sono applicabili... Esiste per ogni, alcuni... da questo si può andare di là, se no, poi ovviamente a meno che non raggiungano localmente uno stato da cui non si possono muovere i sistemi. Allora, da ognuno di questi esisterà una tupla che mi fa muovere in un altro. Cioè, l'essere... non, almeno per quello che riguarda gli LTS, se viene di avere situazioni di *deadlock*, che qui possono occorrere! Perché non in questo modello, ma nel momento in composizione sincrona posso ottenerli anche se non sono processi. Se tutto va bene in questi oggetti qui non può esistere una cosa di questo genere, perché non c'è un concetto di condivisione di niente. E quindi sarà interessante capire che vuol dire se vogliamo sistemi comunicanti. Quando ci interesseranno i sistemi comunicanti, vuol dire che il problema della loro composizione non necessariamente potrà essere correttamente gestito nella semantica, ma dovrà essere gestito prima. Perché la semantica perde... gli LTS perdono il concetto di memoria esplicita, quindi di condivisione.
**Studente:** E invece, per quanto riguarda la composizione non parallela che citava prima?
**Professore:** "Non parallela" che non significa niente... La composizione sequenziale? Un sistema, il secondo...
**Studente:** Sì, no, allora, se necessariamente solo la sequenza, l'operatore di sequenziazione, ma per esempio viceversa nel sequenziale... ah cioè sono operatori di composizione.
**Professore:** La ripetizione è un'operazione sequenziale, tipo quelle che fanno la... per esempio la scelta non deterministica. Il "perché faccio uno, faccio l'altro". Quindi ci sono vari operatori di composizione nel mondo sequenziale. E quello che voglio fare, se vogliono fare come quello di ambiente, la sequenzializzazione... allora per la sequenzializzazione devo avere un concetto di stato terminale, che può essere identificato in vari modi, magari però mi deve permettere di collegare gli stati di uno...