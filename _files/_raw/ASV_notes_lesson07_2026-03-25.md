Siamo arrivati a discutere della fairness, per cercare di controllare un po' il non determinismo che emerge quando effettuiamo la composizione parallela. Dunque, che avevo fatto? Questa è la composizione parallela. Riguardo alla fairness, abbiamo incontrato tre tipi di fairness, ma a un certo punto il professor Mogavaro certamente ha avuto il modo di vedere come si esprimono queste proprietà, per esempio in LPM [LTL], e poi vedremo che (qui non so con chi ancora, perché non so come ci si dividerà), a seconda della logica, ci sono logiche che non sono in grado di esprimere evidentemente questa proprietà. Quindi ci deve essere un altro modo per introdurre questi tipi di vincoli nella fase di analisi, sempre per eliminare, per scartare questi comportamenti che potrebbero falsificare i propri requisiti, senza però che questo determini una presenza di errore nel sistema. E di nuovo, questo lo vedrete con il professor Mogavaro, mentre noi seguiremo quello che abbiamo fatto.

Allora, però, non mi pare di ricordare... abbiamo cominciato a vedere sempre questi esempi, e poi li vedremo magari in dettaglio quando parleremo di questo tool che useremo, per farvi vedere che questi saranno alcuni degli esempi che affronteremo. Quindi vedremo in atto proprio il processo di verifica, però mi servirerebbe che il professore completasse, a quanto mi pare, la discussione di LPM e della sua semantica. Quindi, quando lo vedremo, lo chiederemo per la presenza di errore. Però non la prossima lezione, sì.

Ok, allora, quindi abbiamo visto questi tre... abbiamo visto questo primo esempio di protocollo di mutua esclusione, se vi ricordate, che aveva dei problemi. Abbiamo identificato un problema legato alla presenza di questo ciclo unfair, cioè che non tratta correttamente i processi, ed è questo che ci ha portato all'introduzione di questi concetti di fairness. Di questi tre concetti (unconditional, weak e strong, che sono standard ovviamente), una volta introdotti, uno qualsiasi di questi tre in realtà nel nostro caso avrebbe risolto. Perché, se vi ricordate, qui era un esempio nel quale il ciclo infinito era legato al fatto che l'altro processo non venisse mai schedulato, e quindi è sufficiente che venga schedulato infinite volte (quindi il più debole dei meccanismi di fairness) per garantire che lui potrà eseguire la sua azione, perché sono sempre abilitate. Come, diciamo, qua dentro è sempre abilitata la transizione praticamente di uscita da questo ciclo, e quindi questo avrebbe risolto questo problema qui.

Però c'era un altro problema legato a come è definito questo protocollo. Perché, ovviamente, nel momento in cui, a seconda di come viene schedulato, se viene schedulato A e poi esegue questa azione... ah, no, sì, sì, esatto, esegue questa azione, e l'altro viene schedulato prima che A possa entrare. Quindi i flag si trovano entrambi a *true*, e nel momento in cui uno qualsiasi dei due viene schedulato, si trova che nessuno dei due può proseguire direttamente.

Ok. Però, diciamo, tralasciando questo problema, che in qualche modo semplicemente si può risolvere usando algoritmi che avete visto, per esempio, nei sistemi operativi, che immagino abbiate visto. Sono quelli di mutua esclusione, li avete visti? Sì. Per esempio, l'algoritmo di Peterson l'avete visto? Sì. È simile a questo, però un po' più furbo. Ha due variabili condivise: ha il turno, e ha il flag. Poi, ok, lo vedremo, lo rivedremo. Quindi c'è il modo di risolvere, ci sono vari modi di risolvere il problema della mutua esclusione, e questo qui è un modello... non questo, ma quello che vedremo poi. E verificheremo effettivamente che risolve non solo la mutua esclusione, ma garantisce delle proprietà più interessanti della semplice mutua esclusione, senza nulla di aggiuntivo.

Ok. Allora, però, diciamo così, indipendentemente da questo, c'è un problema del modello che abbiamo fatto qui, che sembra essere piuttosto limitativo come modello, perché qui stiamo modellando dei processi in cui il codice non critico e il codice critico è tutto compattato in un'unica istruzione. Quindi, essenzialmente, la quantità di tempo che passano nelle varie sezioni è unitaria. 

Ora, questo può o non può avere impatto sulle proprietà. Il problema è che potrebbe avere impatto sulle proprietà se un processo può restare più a lungo di un singolo istante di tempo; questo potrebbe determinare una violazione. Una violazione di qualche proprietà, soprattutto quando si mettono e si combinano insieme. E, in sostanza, diciamo così, nel momento in cui verifichiamo che un eventuale protocollo di comunicazione come questo soddisfa le proprietà che ci interessano, nulla ci garantisce che una sua variante (per esempio che aumenta la quantità di istanti temporali in cui il processo passa di qua o di qua) continui a soddisfare quelle proprietà.

Quindi l'impatto ci potrebbe essere. D'altronde, però, non vogliamo neanche definire un modello che sia uno per ogni specifica istanziazione, ma proprio, se volete, un modello per ogni possibile processo che utilizza questo protocollo. Per me sono infiniti. Cioè, non c'è un limite a priori nella quantità di istruzioni che io posso inserire qui, o posso inserire qui, nei vari processi che voglio sviluppare. L'unica cosa di cui in qualche modo posso essere certo, è che almeno questo deve garantire che il numero di istruzioni e il tempo che passa in questa sezione sia finito. Perché se fosse infinito, c'è poco da garantire. O meglio, si può garantire ovviamente la mutua esclusione, ma non si può, per esempio, garantire che ogni processo abbia la sua possibilità di accedere alla sezione critica se lo desidera. Perché è una delle proprietà che uno vorrebbe assicurare nel protocollo di mutua esclusione. 

Quindi, ovviamente, è chiaro che se ammettessimo la possibilità che un processo prenda la sezione critica e se la tenga per sempre, è il processo che è fatto male, non è il protocollo che è fatto male. Quindi, sotto l'assunzione ragionevole che, per esempio, ogni processo spenda tempo finito nelle varie sezioni di codice privato, noi vorremmo appunto verificare che il protocollo è corretto, indipendentemente da quanto tempo impieghi.

Ora, come facciamo però a modellare una situazione di questo genere? Questa classe infinita di sistemi, senza necessariamente spiegarle in un dialogo, richiede invece di fare un solo modello che li abbracci tutti. Allora, questo in realtà si può fare mettendo insieme due cose: cioè, diciamo così, utilizzando da una parte il non determinismo per generalizzare il modello, e dall'altro usare le condizioni di fairness per non allargarlo troppo, non espanderlo troppo. Perché, ovviamente, una volta che c'è il non determinismo si creano problemi di presenza di... diciamo, comportamenti stupidi.

Allora, come potremmo pensare di dire, ad esempio, questo? Pensare di aggiungere delle transizioni che non esistevano nel nostro modello, che permettono a ciascun processo di restare, di eseguire, di evolvere, rimanendo però nella propria sezione. Quindi, queste transizioni (questa che vedete qua e quella che vedete qua) sono due transizioni che sono sempre abilitate e, intuitivamente, modellano l'esecuzione privata di istruzioni. Quindi, il tempo passa, ma continua a restare in questa lunga sezione, critica o non critica, come volete dire. Questo è importante perché, ovviamente, se questo componente fosse inizialmente isolato, non ci sarebbe la presenza o l'assenza di queste e non avrebbe nessun impatto sulle proprietà. Però, questo è interessante nel momento in cui lo metto in parallelismo con un altro processo che può eseguire mentre anche lui lo esegue e, come dire, il fatto che un processo esegua questa azione, pur non modificando lo stato per sé, permette però all'altro di evolvere, modificando lo stato, e quindi ho molti più comportamenti a disposizione da analizzare. 

Ovviamente, questo mi sta da un lato permettendo di modellare processi che possono decidere di eseguire, di stare, diciamo, tante unità di tempo. Qui, ovviamente, il concetto di unità di tempo... questi sono tutti sistemi discreti che stiamo discutendo. Poi, discuteremo anche dei sistemi a tempo continuo, ma questi sono a tempi discreti. Quindi, qui, il concetto di unità di tempo è legato all'esecuzione di un singolo processo. Quindi, ogni esecuzione, ogni cambio (o non cambio) di stato, ogni esecuzione di transizione coincide con un'unità di tempo. Questa è l'unità di tempo elementare. Quindi queste sono, se volete, le azioni atomiche, l'esecuzione di un'azione non scomponibile ulteriormente. E quindi, ovviamente, questo permette di modellare un processo che decide di passare un tempo arbitrario all'interno di una sezione del suo codice, se stiamo parlando di software. Però, da un lato ci permette effettivamente di modellare quello che vogliamo, ma purtroppo ci permette anche, ovviamente, quello che non volevamo: cioè la possibilità che un processo in sezione critica ci resti per sempre, perché quella transizione è sempre abilitata, e lui potrebbe prendere quella per sempre. Quindi, si intrappola lì. Questo, però, è un comportamento che noi non vorremmo ammettere, e quindi dobbiamo avere uno strumento per eliminarlo. Qual è lo strumento per controllare il non determinismo? 

Se ci pensate, quello che abbiamo visto nella lezione passata è esattamente lo strumento che ci può servire, perché, anche se l'abbiamo introdotto per modellare lo scheduling, non è limitato necessariamente allo scheduling. Nel senso che io qua ve l'ho definito come una proprietà legata al meccanismo di schedulazione. Ma se sostituite questa proprietà (la proprietà di essere schedulato) con un'altra proprietà di stato, uno può dire: desidero che i percorsi buoni siano i percorsi in cui qualcosa si verifica infinite volte. Come, quindi, se voglio usare questa, è un'unconditional fairness. Oppure questo dice: se una certa proprietà è vera continuativamente, allora voglio che un'altra proprietà atomica sia vera infinite volte. E avremo una generalizzazione del concetto di weak fairness. E la strong fairness: se una certa proprietà della mia sequenza di stati occorre infinite volte, un'altra deve anche lei occorrere infinite volte. Quindi, qui la proprietà era "enabled" e l'altra era "scheduled". Il fatto che io qua l'abbia calato nel concetto del meccanismo di schedulazione è del tutto arbitrario, nel senso che posso usarla anche in altri contesti. 

E questo è esattamente quello che ci permetterebbe, per esempio, di controllare... dove stiamo? Questo, diciamo, la composizione di questo sistema. Perché, come vi ho fatto notare, ovviamente, quando vado a fare l'LTS, con i due processi definiti in questo modo qua, mi trovo di fronte a delle cose di questo genere. In cui, esattamente, entrambi sono in non critico, A vuole entrare, imposta il suo flag, poi entra, e poi c'è questa transizione che gli permette di restare lì per sempre. Allora, che cosa potremmo fare? Per esempio, per eliminare questo comportamento, potremmo dire che desideriamo (ci basta in questo caso un'unconditional fairness) che ogni processo debba rispettare il vincolo di non trovarsi in sezione critica per un tempo infinito. Il che vuol dire che o resta sempre fuori, o se entra, prima o poi deve uscire, e deve uscire in un tempo finito. Il vincolo di fairness, ovviamente, non impone nessun limite a quanto tempo ci sta. Impone però il vincolo che il tempo debba essere finito. Cioè, il numero di volte in cui esegue una transizione restando sempre lì dentro, deve essere finito. E questo è interessante, perché se ci pensate, aggiungendo questo vincolo a questo processo, ci sta dicendo che se il protocollo soddisfa le proprietà, lo soddisfa per qualsiasi processo. A patto, ovviamente, che questo processo non vada in realtà a modificare nella sezione critica o non critica il flag di A, quindi le uniche operazioni che modificano il flag siano queste qui. Quindi, se astraggo, tutto il codice che c'è qua e tutto il codice che c'è qua non deve avere impatto su queste variabili qui. Ma a questo punto posso fare qualsiasi cosa, non mi interessa se non ha impatto sulle proprietà che io voglio verificare. Allora, questo vuol dire, appunto, che sto verificando una classe infinita di processi, praticamente per qualsiasi processo che io voglia specificare. 

E quindi questo, sostanzialmente, che cosa mi dice? Mi dice come il non determinismo può essere utilizzato per modellare sistemi in presenza di informazione parziale: quindi io non ho ancora deciso come si dovrà comportare, però so quali sono i possibili comportamenti che deve avere. E questo è, in sostanza, quello che sto facendo qua, perché io non so in base a cosa lui deciderà di uscire. Quindi è una forma di mancanza di informazione, oppure (che è abbastanza collegato) una forma di astrazione: sto astraendo da informazioni che magari conosco già, ma che non voglio specificare perché voglio mantenere la dimensione del modello, dell'LTS in particolare, sufficientemente compatta. Ovviamente, come vi ho detto, il non determinismo è l'unico che crea problemi; il modo di solito per ovviare a questi problemi sono ancora le condizioni di fairness, quindi questo è il modo in cui sostanzialmente controlliamo il non determinismo. Ovviamente questo è quello che c'è scritto qua, sostanzialmente ne abbiamo già parlato. 

Allora, qual è il secondo problema di cui uno si deve preoccupare quando fa una modellazione, a cui deve stare attento? È il concetto di atomicità. Questo, ovviamente, è un problema che avete già incontrato nei sistemi operativi ed è legato, sostanzialmente, all'emergere di opportunità di race condition, a situazioni in cui i processi possono, a seconda della velocità in cui operano, determinare comportamenti diversi. Quindi l'ordine in cui vengono schedulati può determinare comportamenti diversi. E questo, ovviamente, è problematico ed è legato al fatto che, come vi ho detto poco fa, il concetto di transizione è esattamente il concetto di operazione atomica e quindi di unità di tempo. Quindi, quando modellate, dovete stare attenti e dovete garantire che ogni transizione corrisponda a qualcosa di atomico o modelli un'azione atomica. 

Perché è facile vedere che, per esempio, una transizione che modella un'azione di incremento in un grafo, potrebbe o potrebbe non corrispondere a un'operazione atomica. Questo dipende da qual è il modello di esecuzione sottostante, giusto? Dipende da cosa state modellando e come si comporta veramente il sistema concreto che state modellando. Perché, se prendete ad esempio questi due processi paralleli che hanno una transizione sola (due transizioni di incremento), è chiaro che a seconda di come questa operazione verrà poi implementata concretamente, questo potrebbe determinare comportamenti molto diversi. Se questa fosse un'operazione atomica, problemi non ce ne sarebbero, perché non importa l'ordine in cui vengono eseguite queste due transizioni, il risultato sarà sempre lo stesso. Ma se queste due azioni in realtà sono implementate con tre istruzioni, l'interleaving tra questi due processi (immaginate che sia un sistema monoprocessore) potrebbe portare a valori ben diversi. Ad esempio potrebbe portare a 2, ma potrebbe anche portare a 1. Perché? Perché entrambi eseguono il loro caricamento (prima carica in R1 A che è 0, viene schedulato l'altro, carica in R2), dopodiché qualsiasi ordine venga seguito nel prosieguo, il risultato sarà 1 perché per entrambi l'incremento porterà a 1 e quindi si sovrascriverà. Che non è il comportamento che nel modello io otterrei se considerassi questo atomico. E quindi, ovviamente, è una cosa che devo tenere in conto quando modello. 

E questo, per esempio, nei sistemi di mutua esclusione può portare di nuovo a diversi tipi di comportamenti. Perché, se immaginate questo (che è diverso da quello di prima)... Questo qui, prima di modificare il proprio flag, verifica che l'altro non abbia prenotato la sezione critica. Nel momento in cui si accorge che la sezione critica è libera, assegna il flag ed entra. Quindi il flag qua indica non il desiderio di entrare ma la mia presenza dentro la sezione. A seconda di come io implemento questa operazione (o come un'operazione atomica o come un'operazione composta da due, il test e il set), i risultati sono ovviamente molto diversi. 

Nel modello a program graph che abbiamo visto, esiste il concetto di transizione che ha una condizione di abilitazione e un'azione che viene eseguita contemporaneamente. Quindi, se io modellassi questi due processi con questo program graph, avrei la possibilità di definire la transizione che passa da qui a qui come un'unica transizione. Mi metto in waiting, P è in questo stato fin tanto che il flag 2 è vero e resta qua. Nel momento in cui questo diventa falso, allora assegno immediatamente il mio flag a true e mi porto nella sezione critica. Se questo viene fatto in questo modo, ovviamente il deadlock non si verificherà più e questo modello qui è anche corretto, perché si garantisce la mutua esclusione. Ma, sfortunatamente, non è detto che questa cosa sia atomica. Io l'ho modellata come se fosse atomica, ma in realtà potrebbero essere due azioni separate. 

E se sono due azioni separate, allora la modellazione del program graph deve essere questa, in cui la fase di ingresso al protocollo in realtà è separata in questo modo: la verifica dell'occupazione e, dopo aver verificato che non è occupato, c'è l'azione di modifica del flag. Questo ovviamente lo devo fare anche per quell'altro. In questa situazione, però, è vero che continua a non esserci più il problema di deadlock come prima, ma nasce un nuovo problema, che è la violazione della mutua esclusione. Perché adesso questo comportamento è possibile: sono entrambi in non critical, A vuole entrare, verifica che l'altro non c'è (il flag suo è falso) e quindi si mette nella seconda parte, va in entry. Qui lui ha già verificato che l'altro non è in sezione critica, e quindi quando verrà rischedulato, lui semplicemente assegnerà il suo flag ed entrerà. Però, nel frattempo, viene rischedulato l'altro e prova a entrare, si accorge che il flag dell'altro ancora non è stato impostato, quindi anche lui si porta nello stato C. Ma da questo stato C, entrambi pensano che sia libera, e quindi entrambi possono entrare in seguito all'altro, violando la sezione critica. E quindi, di nuovo, bisogna stare molto attenti a come si modella. 

Il linguaggio che abbiamo dato permette di modellare, diciamo, gruppi atomici. Cioè, questa è una transizione di cui esiste effettivamente un'implementazione concreta, che si chiama test-and-set, che è sostanzialmente il test e contemporaneamente il set (anche se tipicamente è legato allo stesso hardware). Però, ovviamente, non è detto che sia il modo corretto, perché se questo è il modo in cui verrà implementato effettivamente il protocollo di mutua esclusione, ovviamente avremo dei problemi. Va bene. Allora, questo è un po' quello che volevo dire di questo. Poi riprenderemo il discorso con degli esempi concreti quando avremo il tool.

Invece volevo parlarvi di... Per ora noi abbiamo visto un meccanismo di composizione parallela che è sostanzialmente asincrono e per la comunicazione utilizza variabili condivise, che è un meccanismo, un modo di realizzare la comunicazione. Esistono però altri meccanismi di comunicazione, inclusivamente tramite scambio di messaggi. Come poi questo meccanismo di scambio di messaggi è realizzato, dipende dal contesto di esecuzione. È chiaro che in un contesto, per esempio di singolo calcolatore, questo scambio di messaggi è in realtà implementato con variabili condivise. Ma ovviamente in un sistema distribuito non esiste una variabile condivisa e di conseguenza, se voglio modellare questo, devo avere un minimo di complicazioni, per esempio lo scambio in rete o il bus, quindi ci sono vari modi. 

Quindi se voglio modellare, se voglio avere, diciamo così, un modo naturale per modellare questo tipo di comunicazioni, devo utilizzare un modello diverso di questo fenomeno. Allora, perché insomma, quello che abbiamo non ci permette di fare una sincronizzazione o una comunicazione tra componenti che non sia attraverso una memoria condivisa. Allora, se parliamo di Labeled Transition System (LTS), un modo per modellare la comunicazione tra processi... che è l'unica cosa che possiamo fare in realtà con l'LTS, perché l'LTS non ha nessun concetto di area di memoria o di mezzo di comunicazione. Quindi l'unica cosa che abbiamo a disposizione sono i simboli che fanno evolvere i vari componenti, cioè i simboli che etichettano le transizioni. Che noi abbiamo interpretato come azioni; se vi ricordate possono essere simboli, sono azioni, ma il fatto che siano spesso interpretati come azioni non significa che non possano essere delle azioni di comunicazione. E quindi l'idea di far comunicare due componenti modellati con i Transition System è quella di utilizzare i simboli condivisi. 

Quindi l'idea è: ho due Transition System ciascuno con un insieme di azioni e definisco un insieme di azioni intuitivamente condivise, cioè che ha sia l'uno che l'altro, che rappresentano le mie azioni di sincronizzazione. Quando due processi si sincronizzano, l'idea è che questi due processi fanno gli affari propri. La sincronizzazione, quella che stiamo dicendo qui, è il concetto atomico o elementare, se volete, di comunicazione. Ovviamente in un LTS non c'è scambio di informazione, perché ovviamente non c'è un concetto di informazione. E quindi la comunicazione coincide essenzialmente con la sincronizzazione. Comunico dicendo: "io sono pronto a fare qualcosa e tu sei pronto a fare qualcosa".

Ora l'idea sostanzialmente è che affinché uno dei due possa eseguire un'azione di sincronizzazione, l'altro processo anche lui deve essere in grado di eseguire quell'azione di sincronizzazione che ha lo stesso nome, e questo porta ad una transizione congiunta dei due processi. Quindi non c'è più l'interleaving in quella situazione lì, ma c'è un reale parallelismo, una sincronizzazione un po' come avveniva nella sincronizzazione perfetta che abbiamo introdotto qualche lezione fa, in cui tutte le volte che un processo doveva eseguire un'azione anche gli altri dovevano eseguire una qualche azione. Qui il parallelismo reale ce l'abbiamo solamente su un'azione di sincronizzazione; sulle altre che non sono di sincronizzazione continua a vincere il regime di interleaving. 

Quindi il modello matematico di sincronizzazione via *handshake* (viene detto "stretta di mano") è definito in questo modo: abbiamo due transition system, un insieme di azioni comuni di sincronizzazione, e il transition system ottenuto tramite composizione (scusatemi, è assincrona, ma parzialmente assincrona se volete, usando questo insieme di sincronizzazione) è definito in questo modo. Come al solito abbiamo un concetto di stato globale eccetera, e, se volete, anche di azione globale come abbiamo sempre detto: quindi prodotto cartesiano degli stati, prodotto cartesiano degli stati iniziali, e insieme di azioni che contiene potenzialmente le azioni di sincronizzazione comuni più quelle che abbiamo, diciamo, le azioni congiunte. Questa è una delle possibili... se vi ricordate abbiamo visto due formulazioni equivalenti del prodotto interleaving, una in cui ogni azione erano due azioni e una in cui si faceva l'unione disgiunta, eccetera. Ovviamente io qua ho riutilizzato il primo dei due, ma potremmo anche utilizzare il secondo. L'importante è che le azioni comuni non siano in unione disgiunta, cioè esattamente la stessa pratica. 

Allora le transizioni che cosa diventano? La relazione di transizione della composizione è definita in questo modo: da una coppia di stati tramite una certa azione globale mi posso muovere in un'altra coppia di stati. Sotto quali condizioni? Le condizioni sono diverse a seconda di che tipo di azione è $A$. Se $A$ è un'azione di sincronizzazione, allora questa transizione esiste se, ovviamente, esistono le transizioni nei due processi che vanno da $S_1$ con $A$ in $S_1'$ e da $S_2$ con $A$ in $S_2'$ con la stessa azione $A$. Quindi entrambi devono poter fare, dai due stati di partenza locali, quell'azione. Oppure l'azione non è un'azione di sincronizzazione, e quindi necessariamente è l'azione di uno e l'altro sta fermo: l'uno esegue l'azione $A$ e ovviamente l'altro non si muove di stato, e l'altro si muove con l'azione. Quindi qua questo vi fa capire che se un'azione è di sincronizzazione, entrambi devono eseguire contemporaneamente l'azione. Quindi è un ibrido questo tra il prodotto sincrono e il prodotto interleaving, dove il caso sincrono si applica solo se è un'azione di sincronizzazione. E ovviamente uno può avere un concetto di sincronizzazione multidimensionale che dipende da come lo voglio definire. 

Quindi, con questo concetto di sincronizzazione via handshake, definito in questo modo, posso costruire un meccanismo di mutua esclusione utilizzando un arbitro esterno, quindi un modello di un protocollo di mutua esclusione che usa un processo arbitro esterno. L'idea, per esempio, potrebbe essere quella di avere tre processi: due sono i processi che vogliono sincronizzarsi, cioè due processi che vogliono accedere in mutua esclusione. E qui ovviamente ho fatto una versione molto semplificata in cui abbiamo uno stato (questi sono LTS, non sono program graph), uno stato che rappresenta la sezione non critica e uno stato di sezione critica. Le azioni per passare dalla sezione non critica a quella critica, e viceversa, sono azioni che intuitivamente rappresentano richieste fatte a un arbitro, il quale deciderà se e quando far entrare qualcuno nella sezione critica. Quindi abbiamo delle azioni di request e di release: l'azione di request è la richiesta per entrare, e l'azione di release è l'azione in cui informo l'arbitro che sto rilasciando la sezione critica. Quindi i due processi sono esattamente identici da questo punto di vista, fanno le stesse cose. 

Il loro prodotto interleaving, che vedete qua, è questo. Se però io poi voglio, diciamo così, definire un prodotto handshake con l'arbitro, posso costruire questo prodotto interleaving. Come vedete, nel momento in cui faccio il prodotto interleaving questi processi fanno quello che vogliono e quindi ovviamente c'è lo stato in cui entrambi entrano in sezione critica perché non c'è nessuno che lo impedisca. Però se adesso prendo questo e faccio, diciamo così, una composizione di questo sistema che ho costruito con un arbitro, dicendo che le azioni condivise tra questo processo e l'arbitro sono la *release* e la *request*, allora il modello che viene fuori è quello che vedete qua sotto. Perché? Perché ovviamente quando questo processo qua che ho costruito vuole eseguire una *request*, poiché l'azione di *request* è condivisa con l'arbitro, l'arbitro deve far eseguire la sua azione. Quindi, per esempio, se questo da qui si muove qui con una *request*, questo ovviamente forza l'arbitro a muoversi in $L$. Nel momento in cui l'arbitro è in $L$, l'unica azione che è eseguibile per il processo è la *release*, perché la *release* è ancora un'azione di sincronizzazione. O meglio, la *request* è un'azione di sincronizzazione, quindi questa transizione qui non può essere eseguita, perché di qua l'arbitro non è pronto a fare una *request*, non può accettare una *request*. L'unica cosa che accetta è una *release*, quindi questo forza il sistema a muoversi qua prima che l'altro processo possa eseguire una nuova *request*. Quindi nel momento in cui io eseguo la composizione usando il modello che abbiamo visto prima tra questo e questo, quello che ottengo è questo LTS in cui ovviamente il comportamento è quello che desidero, e il problema della mutua esclusione viene di nuovo risolto. 

Quindi per distinguere la composizione sincrona dalla sincronizzazione via handshake, tipicamente a questo operatore viene passato un parametro che indica l'insieme delle azioni di sincronizzazione. Ovviamente se questo insieme è vuoto otteniamo l'interleaving classico. Quindi, in sostanza, potete usare lo stesso operatore sull'insieme vuoto (cioè non c'è nessuna sincronizzazione mai), ma poi il secondo vincola la *release* e la *request* ad essere operazioni di sincronizzazione. E quindi questo è un altro possibile modo di modellare i protocolli di sincronizzazione. 

L'ultima cosa che volevo farvi vedere è, diciamo, una generalizzazione di questo modello via handshake portato a livello di program graph (noi abbiamo visto solo il program graph con memoria condivisa) che introduce il concetto di sistemi di comunicazione via canale. Questi vengono chiamati Channel Systems, o sistemi a canali, e sono sostanzialmente dei program graph. Se vi ricordate come sono definiti, ogni program graph è definito con una serie di variabili, giusto? Quindi, per gli scopi di quello che è stato scelto qui per illustrare questo modello, io assumerò che le variabili siano solo locali, che non ci siano variabili condivise tra processi. Ovviamente, posso avere dei sistemi con una modellizzazione ibrida in cui ci sono sia variabili condivise che canali, o variabili condivise tra alcuni processi e canali di comunicazione per altri, perché magari una parte dei componenti sono localizzati sullo stesso nodo (cioè hanno a disposizione memoria condivisa) e altri sono distribuiti all'esterno. È chiaro che poi, nel momento in cui voglio modellare, posso usare dei modelli ibridi in questo senso. Ma per gli scopi della presentazione di questo modello di comunicazione via canali, assumerò semplicemente che le variabili siano locali. Quindi ogni processo avrà le sue variabili e, anche se hanno lo stesso nome, poi faremo, quando costruiremo la composizione, l'unione disgiunta delle variabili, ok?

Però, in aggiunta (quindi sono proprio i program graph classici), quando eseguo la composizione tra questi prevedo la presenza di canali di comunicazione che sono utilizzabili da tutti. Di nuovo, è il modello semplificato che vi do; esistono ovviamente raffinamenti di questo modello in cui ci sono canali usabili solo da sottogruppi di processi e non accessibili ad altri. Ovviamente si può generalizzare o raffinare come volete. Quindi abbiamo questi oggetti condivisi tra i processi, questi nomi condivisi, e a ciascuno di questi è associata una capacità. Una capacità vuol dire quanta memoria possono tenere, quanti messaggi possono ricordare. E ovviamente la capacità di un canale può essere anche zero, cioè un canale senza memoria. L'idea intuitiva ovviamente è che ognuno di questi canali sia  semplicemente modellato come una coda FIFO, per cui il primo messaggio che viene inserito sarà il primo che dovrà essere consumato in lettura. Ovviamente questo vuol dire che devo prevedere delle operazioni di scrittura e di lettura di questi canali. Quindi leggo dalla testa e scrivo in coda. 

Quando però la capacità è zero, vuol dire che non esiste essenzialmente memoria, non c'è persistenza dei messaggi, e quindi i canali con capacità zero corrispondono a meccanismi di sincronizzazione via handshake. Richiedono cioè che il messaggio venga comunicato istantaneamente da un processo all'altro, e quindi entrambi devono partecipare a questa comunicazione contemporaneamente, ok? Altrimenti, se la capacità è maggiore di zero, fin tanto che c'è spazio ovviamente i due processi possono accedervi in maniera asincrona, ok?

Ora, poiché però questi sono degli oggetti nuovi, il modo in cui i processi interagiscono con questi non è assegnando dei valori, ma eseguendo delle azioni di scrittura e lettura che vengono indicate da questi due simboli: il punto di domanda corrisponde alla lettura e il punto esclamativo all'operazione di scrittura.O se volete, questa è la ricezione di un messaggio da un canale e questa è l'operazione di invio di un messaggio su un canale, ok?

E quindi, diciamo, la sintassi è tipicamente questa: quando voglio ricevere un messaggio da un canale, dico da quale canale voglio riceverlo (che voglio fare una ricezione) e dove voglio inserire il risultato della lettura, cioè il messaggio che ho letto.Quindi $x$ è una variabile locale in cui verrà scritto il messaggio.Questa, ovviamente, è l'operazione più semplice possibile; ce ne sono di ben più complicate, alcune delle quali vedremo quando parleremo del sistema di verifica concreto.Quindi per ora sono messaggi atomici che possono essere letti così come sono.Questa invece è l'operazione di invio di un messaggio: in questo caso il messaggio è $d$ e $d$ è un valore appartenente al dominio, quindi è una costante in sostanza, e quindi sto inviando questo valore sul canale.Anche questa, diciamo, è molto elementare per un'operazione.In generale troveremo che le operazioni saranno, se possibile, delle cose fatte in questo modo, dove l'espressione appartiene alle espressioni del mio program graph (può contenere variabili, operazioni, eccetera).E ovviamente l'idea è che questa operazione qua in realtà coinvolge la valutazione dell'espressione prima.Quindi semanticamente devo andare a vedere i vari componenti, qual è la loro interpretazione, eseguire le operazioni e il risultato che otterrò (chiamiamolo, per esempio, il risultato di valutare in un determinato stato, cioè data una certa $\eta$ che dà alle variabili i valori che si stanno trovando) sarà un oggetto che appartiene a $d$ e quindi può essere inviato in qualche modo.Ma, diciamo, per mantenere semplice la presentazione qui, io vi do direttamente una costante.Ok, allora va bene, questi sono stile send tra invio e ricezione. 

E insieme, l'idea è che quando compongo, o meglio, questi program graph hanno in realtà a disposizione questo insieme di azioni.Per ogni canale che conoscono e per ogni variabile e valore del dominio, hanno a disposizione un'azione di questa forma.Quindi sto aggiungendo azioni che loro possono usare, oltre alle loro private, che sono definite in questo modo qua.Ovviamente mi sono forse dimenticato di dirvi che non solo ad ogni canale è associata una capacità, ma in linea di principio può essere associato un dominio, cioè quali sono i valori che sono in grado di accettare. Ricordate che noi abbiamo un grosso dominio che può contenere tutto, ma in realtà tipicamente questo dominio può essere visto come partizionato in sottodomini.E se io per esempio voglio un canale in cui mando solo certi tipi di messaggi, allora il sottodominio dei valori di quel tipo sarà parte del mio dominio.Ma ovviamente posso limitare sostanzialmente i messaggi che posso inviare su un canale a un altro tipo.Quindi quello che mi serve affinché questa sia ben definita è che il dominio di $C$ deve essere incluso nel dominio della variabile in cui io voglio scrivere il messaggio, e ovviamente questo deve appartenere al dominio di $C$.Anche se non l'ho scritto, ve lo faccio dire adesso, ma queste sono tutte varianti che uno può pensare e utilizzare quando definisce il nostro linguaggio o utilizza il nostro linguaggio.Allora, come avviene... quindi qual è la definizione sintattica?

Allora, l'unica cosa che viene aggiunta è questo insieme come insieme di azioni che un singolo programma può eseguire.E il channel system (il sistema di canali) è definito sull'insieme delle variabili che è l'unione disgiunta delle variabili dei processi più i canali che possono utilizzare.Ora vediamo com'è definita a questo punto la semantica di questa, cioè come unico LTS risultante.Allora, la prima cosa che dobbiamo fare è definire qual è l'effetto delle azioni nel sistema di comunicazione, quindi dobbiamo dire cosa succede quando viene eseguita un'azione di comunicazione.Quindi abbiamo delle transazioni di questo genere che sono possibili nei vari program graph e, come vi ho detto, l'idea è che se c'è un'azione di questo genere su un canale a capacità zero, deve esistere un'azione corrispondente ad un altro processo (di lettura sullo stesso canale) e queste due transazioni si devono eseguire contemporaneamente, e quindi c'è la sincronizzazione.Altrimenti, se la capacità è maggiore di zero, queste azioni sono autonome, indipendenti.C'è una guardia implicita che è legata alla pienezza o vuotezza, a seconda del tipo di azione, del buffer.Quindi, per esempio, un'azione di scrittura è eseguibile solamente se la capacità del canale non è già piena; altrimenti l'azione diventa praticamente bloccante, quindi non è eseguibile.È come se ci fosse una guardia implicita che dice: "se il canale non è pieno, esegui questa azione".E quindi questo ovviamente vuol dire che, qualsiasi sia la situazione del canale prima dell'azione, rappresentata da questa sequenza di messaggi presenti, dopo l'esecuzione dell'azione si accoda in fondo il nuovo messaggio.E chiaramente per l'azione di lettura, la sua semantica intesa è che se il canale non è vuoto può essere eseguita e, ovviamente, consuma il canale, consuma il messaggio (il primo messaggio che viene letto) e questo messaggio viene assegnato alla variabile.Questa è la semantica intesa che dobbiamo catturare matematicamente.Questo dobbiamo dire: come si cattura, cercare di capire come modellare questa intuizione dentro la semantica.

Ovviamente l'LTS sarà una generalizzazione di quello che abbiamo già visto, perché il concetto di variabile l'abbiamo già visto quando abbiamo parlato di program graph.Quindi, se vi ricordate, da un program graph allo stato dell'LTS corrispondente c'è una coppia: locazione e valutazione delle variabili.Quindi questo è dove si trova nel controllo e questo è lo stato dei dati.Ora, qui noi abbiamo qualcosa in più: oltre alle variabili nei processi, abbiamo anche i canali.E i canali contengono memoria, sostanzialmente sono una specie di variabile condivisa su cui posso eseguire solo certi tipi di azioni che sono quelle che abbiamo visto.Allora, per fare questo, il concetto di stato sarà una valutazione delle variabili e una valutazione dei canali.Quindi se $\eta$ è una funzione dall'insieme delle variabili di tutti i processi al dominio che associa un valore ad ogni variabile, c'è una funzione $\xi$ che associa ad ogni canale una sequenza finita di elementi che sono i messaggi.Vi ricordate che questo qui, $D^*$, è l'insieme delle sequenze finite.Ora ovviamente, a seconda del canale, le sequenze non sono tutte un'infinità di sequenze di lunghezza arbitraria.Chiaramente se volessi definire la $\xi$ più correttamente matematicamente dovrei dire che, fissato il canale che prendo in input, questo va in $D$ elevato a $\le capacità(C)$.È chiaro che cosa sto scrivendo?Sto dicendo che il codominio in realtà dipende dall'input perché canali diversi hanno capacità diverse.Siccome se non so qual è il canale non posso dire quali sono le sequenze che mi si stanno presentando, un modo per descrivere formalmente il fatto che il codominio dipende da... è diverso per valori diversi che stanno dentro *chan*, è quello di dire: "guarda, questa è la funzione che ha come dominio *chan* e come codominio ha questo oggetto qua che dipende da che cosa ha ricevuto in ingresso la funzione", quindi metto il parametro specifico $C$ che è l'input della funzione.E poi posso dire che il codominio sarà l'insieme di tutte le stringhe che sono minori o uguali della capacità di quel canale.Se volessi essere preciso scriverei matematicamente in questo modo la definizione della funzione $\xi$.Però in genere questo è sufficiente anche se contiene molta più roba di quello che veramente può assegnare la funzione. 

Allora, quindi uno stato dell'LTS invece sarà una cosa di questo genere, dove questa è una locazione globale ovviamente (sarà quindi una sequenza di locazioni, una per ogni processo che ho), questa è una valutazione su tutta l'unione disgiunta delle variabili dei processi, e questa è una valutazione dei canali.Allora a questo punto abbiamo questo oggetto qua.Allora quindi, se io ho messo questa semplicità come sequenza di locazioni locali, uno stato sarà una cosa di questo genere come abbiamo visto qua.E ok, va bene, il resto è tutto chiaro lì.Vi ricordate che noi abbiamo, diciamo in LTS esattamente, condizioni iniziali e condizioni iniziali per i processi, questa è sempre la stessa cosa.Quindi diciamo che gli stati iniziali saranno locazioni iniziali accompagnate da una $\eta$ che soddisfa tutte le condizioni iniziali.Non è scritto bene qua perché $\eta$... ah no, sì sì, però non c'è qua... qua c'è una $i$, e quindi deve essere in realtà per ogni $i$ appartenente a $1..n$.Quindi questo vuol dire che deve soddisfare la congiunzione di tutte le condizioni iniziali.E per ogni canale la $\xi$ deve associare $\varepsilon$, la stringa vuota, quindi inizialmente sono vuoti i canali, ok?E mi sembra di chiamarla qua... chiamerò questa esattamente questa valutazione (potrei chiamare anche $\eta_0$).Beh, non è unica diciamo così, queste non sono uniche, mentre questa è unica perché è l'unica che dà $\varepsilon$.

Allora il transition system sarà ovviamente definito in questo modo qua: tupla di locazioni locali, valutazione delle variabili e valutazione dei canali.L'insieme delle azioni sarà l'unione disgiunta delle azioni locali più un'azione nuova che chiamiamo $\tau$ che viene sostanzialmente usata per modellare l'handshake, ok?Quindi ogni transizione etichettata con $\tau$ sarà una transizione in cui si sono sincronizzati nell'LTS, la sincronizzazione di due processi componenti del sistema di canali.E gli stati iniziali li abbiamo appena definiti.L'etichettatura che è questa, è sempre la solita, e quindi qua non ho aggiunto niente rispetto a quello che abbiamo fatto per i program graph.E le transizioni invece, cioè la relazione di transizione, è definita in un certo senso come ce l'aspettiamo.

Quindi questa è la definizione delle transizioni indotte da azioni locali: affinché esista questa... se c'è questa transizione nell'$i$-esimo transition system, e l'azione $A$ appartiene a quella dell'$i$-esimo transition system, e $\eta$ è una valutazione delle variabili che soddisfa la guardia $G$ che sta qua, allora esiste una transizione da questo stato a questo stato in cui, vedete, l'unica locazione che cambia è quella dell'$i$-esimo che porta in una $\eta'$ che è ottenuta dalla funzione di effetto della transizione $A$ che abbiamo definito precedentemente.E ovviamente che non modifica la $\xi$, la valutazione dei canali, perché non è un'azione che li modifica.Quindi questa qui è la classica transizione che già conosciamo.

Quelle cose nuove sono queste qui. Allora, le prime due (questa e questa) sono azioni che come transizioni... ah no, l'ho sempre messa, no, ogni azione di comunicazione è poi tradotta in una $\tau$, che sia eccezionale o no. Ah, quindi se c'è un'azione nell'$i$-esimo della forma $G \Rightarrow c!v$ (cioè una *send*), c'è una $\eta$ che soddisfa $G$, e la capacità di $c$ è uguale a $k$, la lunghezza e l'occupazione di $c$ è minore di $k$ (perché c'è spazio per poter scrivere), e questa è la configurazione corretta del canale (cioè la valutazione del canale in questa sequenza di $k$), allora c'è una transizione da qui a qui in cui, di nuovo, solo l'$i$-esimo cambia stato. La $\eta$ non cambia perché in una scrittura, in una *send*, non cambiano le variabili, ma cambia la valutazione dei canali, semplicemente modificando la valutazione dell'unico canale.  

Quindi questa, ve lo dico, questa qui, $\sigma$, è in realtà una computazione. $\xi'$ è definita in questo modo: $\xi'(x)$ dove $x$ appartiene a $C$ è uguale a $\xi(x)$ se $x$ è diverso da $c$, ed è ovviamente uguale a $\sigma$ se $x$ è uguale a $c$. Quindi questo è semplicemente un modo abbreviato per dire che c'è una nuova valutazione che è uguale a quella di prima su tutti i canali che non sono $c$, e invece restituisce $\sigma$ (cioè quello che vi ho detto di assegnare a $c$) per questo. E qui ovviamente questo è esattamente quello che sta facendo qua. Questo qui sta dicendo che $\xi'$ è uguale a $\xi$ a cui, a $c$, aggiunge una $\sigma$, dove la $\sigma$ è questa sequenza: come vedete, aggiunge un po' in coda.  

Di contro, abbiamo quest'altra che cosa fa? Quest'altra è un po' più complicata perché modifica anche la $\eta'$, perché c'è un assegnamento alla variabile $x$ del valore letto che sta in testa al canale. Quindi il messaggio è (se non è vuoto) la lunghezza... e ci sono esattamente... il primo elemento andrà a finire nella... cioè la $\eta'$ lo inserirà nel valore della variabile $x$. E la $\xi'$, ovviamente, assegnerà al canale il resto della sequenza.  

E infine (e poi tutto il resto sono le varie condizioni, la $c$ deve essere soddisfatta dalla $\eta$ di partenza), quando invece la capacità è vuota, è nulla, allora abbiamo la situazione in cui, esatto, devono esistere ovviamente due transizioni, una per $i$ e una per il processo $j$, che hanno nelle loro guardie una l'azione di lettura su un canale, e l'altra la scrittura sullo stesso canale di un valore. E devo avere una $\eta$ che soddisfa entrambe le guardie, e $i$ e $j$ devono essere diversi. Allora esiste una transizione nell'LTS etichettata con $\tau$ in cui i due processi cambiano la loro locazione d'accordo con quello che c'è scritto in queste due transizioni. La $\xi$ non cambia, perché ovviamente questo canale ha capacità zero, quindi non avviene niente, perché la $\xi$ dà solamente una semantica al canale che è vuoto. Però la $\eta'$ cambia perché avviene il trasbordo del valore $v$ che uno dei due vuole inviare all'altro nella variabile, che è esattamente quello che ci aspettavamo.  

E quindi qua stiamo in realtà, a differenza di quello che abbiamo visto nella composizione parallela di program graph dove non c'era il canale di comunicazione (quindi se vi ricordate abbiamo preso i program graph, abbiamo fatto la composizione parallela di program graph e poi abbiamo fatto la traduzione in LTS), qui in realtà non c'è un concetto di divisione di variabile, ma in un certo senso abbiamo questo concetto di canale separato che abbiamo inserito come oggetto aggiuntivo. Possiamo fare la composizione diretta, cioè una traduzione di un insieme di program graph con canali direttamente dentro l'LTS. Quindi non abbiamo fatto una composizione...  

Quindi diciamo così, mentre prima avevamo program graph 1, program graph $n$, abbiamo prima costruito la composizione parallela nei program graph, e poi questo l'abbiamo tradotto in LTS nel modo normale (una volta che c'è un singolo program graph la traduzione in LTS è immediata). Quindi in realtà abbiamo fatto una cosa diversa: abbiamo preso program graph 1, program graph 2... program graph $n$ con l'insieme di canali, e abbiamo, diciamo così, costruito una funzione di traduzione diretta in LTS senza passare attraverso la composizione. Questo ovviamente non è un problema fin tanto che i program graph non hanno variabili condivise. Perché? Perché la parte di condivisione sta qui, e la tratta in maniera indipendente, cioè non so come tradurlo direttamente qui.  

Ovviamente se avessi delle variabili condivise oltre al canale di comunicazione, dovrei fare un ibrido tra queste due traduzioni: prima tradurre questo come ho fatto qua per la componente relativa all'interleaving, e poi, usando i canali, fare la traduzione che si occupa dei canali. O se volete, posso sempre fare una traduzione unica, ma devo tener conto che non posso semplicemente... no scusatemi, devo fare una traduzione, no, come questa per forza, è tutto insieme, e poi fare la componente della composizione. Il problema era quello, era che non potevo prendere questi, costruire gli LTS e poi fare le composizioni, ma ovviamente posso... qua potevo passare attraverso questo e fare l'LTS, qui non lo posso fare perché ovviamente non esiste il concetto di composizione tra cose che c'hanno i canali, perché lo faccio già trasformando questi canali in variabili condivise, oppure definisco una funzione diretta che fa tutto insieme. Quindi questo non è facilmente spezzabile come abbiamo visto qua, lo devo fare necessariamente. 

Va bene, allora questo è in sostanza quello che volevo farvi vedere in termini di problemi di modellizzazione e linguaggi di modellizzazione. Ora abbiamo ancora un po' di tempo, che cosa... allora, adesso in realtà io non posso farvi vedere... potremmo cominciare con l'esempio del tool che vi devo far vedere, perché una volta finito questo si può andare più lenti. La parte di logica la deve fare il professore perché l'ha cominciata lui, e quindi quello che io posso fare è iniziare a parlare, continuare con la modellazione, ma fare un modello più interessante, non più interessante ma interessante, e poi torneremo indietro dopo. Questi sono i problemi di sincronizzazione che, come ovviamente vi avevo anticipato, sarebbero stati... dovrebbero essere stati... diciamo che si può creare una descrizione. Grazie mille. 

Questo modello è un modello molto speciale perché si occupa solamente di definire sistemi il cui comportamento dipende dal tempo.Quindi l'unica quantità che non è discreta sarà il tempo.Ma ovviamente esistono generalizzazioni di questi modelli in cui le quantità non discrete sono sempre reali, non solo relative al tempo ma, come vi dicevo, potrebbe essere la temperatura, la concentrazione di una sostanza in un certo momento del tempo, eccetera. E l'idea quindi che cos'è? 

Di prendere questi sistemi discreti che abbiamo visto e, diciamo così, iniettare al loro interno delle variabili che non evolvono più in maniera discreta sotto il controllo del componente digitale, ma evolvono autonomamente nel tempo.In un certo senso, ovviamente, il tempo in sé evolve linearmente con se stesso, anzi c'è proprio un'identità secondo una funzione identità.Ma per esempio la temperatura potrebbe evolvere secondo una legge che dipende dal tempo e dalle condizioni, quindi potrebbe esserci una legge di evoluzione per la concentrazione di radiazioni, o meglio della quantità di particelle, eccetera.Quindi esistono delle leggi: la velocità in un'automobile diventa un'altra quantità non discreta che evolve nel tempo in base all'accelerazione e ad altri valori insomma, la posizione dipende dalla velocità, eccetera.E quindi, in linea di principio, l'idea è di avere un... chiamiamolo pure LTS (anche se poi in realtà saranno automi, ma vedremo intanto la corrispondenza tra automi e LTS, per cui ormai potrebbe non essere più un problema) a cui vengono aggiunte delle variabili continue. 

Nel nostro caso ci sarà un'unica variabile continua, che è la variabile tempo, e dovremmo ovviamente avere un modello che ci permette di descrivere il comportamento nel tempo.Nel momento in cui introduciamo il tempo reale, abbiamo una variabile in cui il dominio non solo è infinito, è addirittura non numerabile, ha la potenza del continuo.Il che significa che nel momento in cui io prendo un modello più il tempo e lo voglio tradurre in un LTS che posso analizzare... quindi, se aggiungo variabili poi devo togliere le variabili per costruire l'LTS sottostante.E allora, se ci pensate, nel momento in cui noi facciamo questa traduzione le variabili vengono codificate dentro delle valutazioni. 

Giusto? Quante sono le valutazioni?Sono tutte le funzioni possibili che vanno da $Var$ al dominio delle variabili.Ora, noi abbiamo un'unica variabile che ci interessa, che è $t$ (il tempo), e quindi sono tutte le funzioni che mappano $t$ in $\mathbb{R}$ maggiore o uguale a zero, giusto?  Quante sono queste funzioni qua? Sono molte di più. Nello specifico, guardate, la regola è: il numero di funzioni da $A$ a $B$ quante sono?Sono la cardinalità di $B$ elevata alla cardinalità di $A$.Quindi sono la cardinalità di $\mathbb{R}$, giusto?  $\mathbb{R}$ maggiore o uguale a zero, elevato a uno.Ma queste qui, ovviamente, sono un numero continuo, certo?  Questo cosa significa?Significa che questo oggetto qua avrà almeno questi stati, uno per ogni valutazione del tempo, ok? E questo non solo implica che è infinito lo spazio degli stati, ma è addirittura continuo lo spazio degli stati.  

E quindi entrano in gioco tutti i problemi legati alla decidibilità dell'analisi di questi sistemi.Perché fin tanto che il sistema LTS è finito o può essere, diciamo, astratto in un oggetto finito (cosa che tecnicamente posso quasi sempre fare fin tanto che sto parlando di oggetti digitali concreti, perché l'insieme degli stati è finito, quindi i vari domini sono finiti, ed è sempre tutto potenzialmente rappresentabile ed esplorabile), non è più così quando ho questo oggetto.E quindi in linea di principio non è detto che sia decidibile il problema dell'analisi. 

Ora noi introdurremo il modello che si chiama Automi Temporizzati (Timed Automata): quindi sono degli automi il cui comportamento dipende dal tempo.Voi sapete tutti cos'è un automa, gli automi su parole sono cose che conoscete, no?  Quindi una parola e un automa che riconosce una parola.Ovviamente qua riprenderò sostanzialmente questi concetti.Poi, ovviamente, una volta che abbiamo il concetto di automa temporizzato, il passaggio alla modellazione sarà semplice, cioè modellare sistemi con questi oggetti. Questo è un modello interessante perché entrano in gioco delle questioni di modellazione non banali che non abbiamo visto prima.E poi c'è il problema della decidibilità: in generale poco è decidibile su questi sistemi, o meglio, dipende dalle proprietà che ci voglio verificare sopra. 

Alcuni linguaggi che voi vedrete con il professore, così come sono, sono decidibili.Perché vedremo che è possibile decidere: cioè, se non parlo del tempo in modo esplicito nella proprietà ma solo nel modello, allora è decidibile.Il problema della decidibilità diventa problematico nel momento in cui voglio parlare esplicitamente del tempo.E ci sono alcune logiche con cui ne posso parlare in maniera più o meno approfondita.In generale, nel momento in cui ho stati infiniti, il problema della decidibilità diventa quasi sempre problematico e si perde, a meno che non ci sia un modo di costruire una rappresentazione finita che preservi le proprietà che io voglio verificare.E questa rappresentazione finita si chiama quoziente. 

Il quoziente è esattamente lo stesso concetto che abbiamo in algebra.Come vi ricordate, in algebra c'è un concetto di quoziente rispetto a una relazione di equivalenza, giusto? Quindi, poter costruire un oggetto finito, un quoziente finito, tramite un concetto di relazione di equivalenza tra stati che intuitivamente prenderà infiniti stati che sono equivalenti dal punto di vista della proprietà che io voglio verificare e li metterà tutti insieme.E quindi modellerò sostanzialmente, rappresenterò esplicitamente insiemi di stati, o se volete, classi di equivalenza diverse, perché sono proprio situazioni del sistema che sono distinguibili dal linguaggio dell'ambiente. 

E quindi ci concentreremo sulla raggiungibilità.Com'è esprimibile la proprietà di raggiungibilità in LTL (Linear Temporal Logic), l'avete vista?  Che cosa conoscete in LTL come operatori?Avete le proposizioni atomiche, il linguaggio logico (AND, OR, implica, eccetera), ma poi ci sono gli operatori temporali. Gli operatori temporali li conoscete: $\square$ (Globally), $\Diamond$ (Future), $\bigcirc$ (Next), $\mathcal{U}$ (Until).Quello che ci interessa è questo qua, il $\Diamond$ (Future/Finally). Perché qual è la semantica intesa di $\Diamond P$ nella mia esecuzione?Se io mi trovo qua e chiedo se questa proprietà è vera, qui deve esistere un punto più avanti che dice che $P$ è vera.Quindi questo, se ci pensate, è esattamente la proprietà di raggiungibilità.È vera se è raggiungibile uno stato in cui è vero quello che sto cercando, mi sta dicendo che è raggiungibile uno stato con questa proprietà. 

Quindi la raggiungibilità è esprimibile in maniera abbastanza semplice. Come vedete, questo problema è decidibile in questi sistemi.Quando però questa proprietà è una proprietà che parla di locazione, cioè: "è raggiungibile prima o poi questa locazione del sistema?".Se poi comincio a chiedermi: "è raggiungibile questa locazione *entro* questo tempo?", questo già è più difficile da decidere.Però vedremo, in realtà dipende.Se abbiamo tempo, potrò farvi vedere una logica che invece parla specificamente del tempo, ma non è quella lineare.È in realtà una logica sul tempo che estende una logica che si chiama CTL (Computation Tree Logic).Non so se l'avete già vista col professore, ma è una logica branching time (a tempo ramificato), e questa può essere estesa con vincoli del tipo "entro un certo tempo" o "in un certo intervallo", ed è comunque decidibile la verifica di questo. 

Questo è quello con cui vorrei cominciare.A questo punto comincio giusto con un'introduzione di questo modello.L'utilità è appunto per la modellazione di sistemi, quelli che vengono tipicamente chiamati sistemi *embedded*, perché sono immersi in un ambiente analogico.Quindi sono tipicamente i controllori: tutti i controllori sono sistemi di questo genere, controllano il mondo reale e ovviamente il loro comportamento dipende da quantità che a loro volta dipendono dal tempo.Fortunatamente ci sono dei termini che ancora non conoscete, che vedrete col professore, e va bene. 

Quello che vi farò vedere... partiremo da un esempio, da un contesto che voglio modellare, che è quello che vi dicevo dell'incrocio ferroviario.E cercheremo di capire quali sono i problemi, cosa un modello stile LTS come quello che abbiamo visto non permette di modellare, perché è necessario modellare adeguatamente un concetto di tempo, e vedremo poi quali sono i problemi che emergono quando introduciamo questo concetto di tempo. Va bene, chiudo qua perché sono le 18:20.Settimana prossima abbiamo ancora lezioni per via della Pasqua e quindi non ne perdiamo nessuna.

ciao